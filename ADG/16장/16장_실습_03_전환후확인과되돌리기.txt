================================================================================
 제16장 실습 03 — 전환 후 확인과 되돌리기
 대상   : boston-srv (새 Primary), chicago-srv
 목표   : 파라미터가 어떻게 다시 쓰였는지 확인하고 원래 역할로 되돌린다.
          되돌리는 것까지가 훈련이라는 것을 익힌다
 소요   : 약 30분
 변경   : 역할을 원래대로 되돌린다
================================================================================

--------------------------------------------------------------------------------
 [1] 파라미터가 뒤바뀌었다
--------------------------------------------------------------------------------

SYS@boston> COL value FOR a60
SYS@boston> COL name FOR a24
SYS@boston> SELECT name, value FROM v$parameter WHERE name IN
  2            ('log_archive_dest_2','log_archive_dest_state_2',
  3             'fal_server','log_archive_config');

NAME                     VALUE
------------------------ ------------------------------------------------
log_archive_dest_2       service="chicago", ASYNC NOAFFIRM delay=0 optional
                         compression=disable max_failure=0 reopen=300
                         db_unique_name="chicago" net_timeout=30,
                         valid_for=(online_logfile,all_roles)
log_archive_dest_state_2 ENABLE
fal_server
log_archive_config       dg_config=(chicago,boston,farsync)

4 rows selected.

-- ★ service="chicago" 다.
--   전환 전에는 chicago 가 boston 을 가리켰는데 이제 반대가 됐다.
--   Broker 가 다시 썼다.
--
-- ★ 7장에서 손으로 구성할 때를 떠올린다.
--   양쪽 파라미터를 사람이 맞춰 적었고
--   전환하려면 그것을 다시 손봐야 했다.
--   Broker 는 그 일을 전환 과정에 포함한다.
--
-- ★ FAL_SERVER 가 비어 있다.
--   Broker 는 이 파라미터에 의존하지 않고 구성 정보로 갭을 해소한다.
--   13장에서 인수 직후에 확인한 그대로다.
--
-- ★ LOG_ARCHIVE_CONFIG 에 farsync 가 남아 있다.
--   10장에서 Far Sync 를 구성할 때 들어간 값이며 지금은 쓰이지 않는다.
--   동작에는 지장이 없지만 정리 대상이다.

SYS@boston> COL destination FOR a14
SYS@boston> COL error FOR a30
SYS@boston> SELECT dest_id, destination, status, error
  2            FROM v$archive_dest_status WHERE dest_id IN (1,2);

   DEST_ID DESTINATION  STATUS    ERROR
---------- ------------ --------- --------
         1              VALID
         2 chicago      VALID

2 rows selected.

-- ★ 15장에서 익힌 뷰로 확인하면
--   목적지 2번이 chicago 를 가리키고 VALID 다.

--------------------------------------------------------------------------------
 [2] Broker 의 화면
--------------------------------------------------------------------------------

DGMGRL> show configuration

Configuration - dg_chicago

  Protection Mode: MaxPerformance
  Members:
  boston  - Primary database
    chicago - Physical standby database

Fast-Start Failover:  Disabled

Configuration Status:
SUCCESS

-- ★ 구성 이름은 dg_chicago 그대로다.
--   13장에서 지은 이름이며 역할과 무관하다.
--   헷갈릴 수 있으므로 실무에서는
--   역할을 암시하지 않는 이름을 쓰는 것이 낫다.
--
-- ★ 들여쓰기도 뒤바뀌었다.
--   15장에서 본 V$DG_BROKER_CONFIG 의 REDO_SOURCE 열이
--   이 들여쓰기를 열로 표현한 것이다.

DGMGRL> validate database 'chicago'

  Database Role:     Physical standby database
  Primary Database:  boston

  Ready for Switchover:  Yes
  Ready for Failover:    Yes (Primary Running)

  Managed by Clusterware:
    boston :  NO
    chicago:  NO
    Validating static connect identifier for the primary database boston...
    The static connect identifier allows for a connection to database "boston".

  Log Files Cleared:
    boston Standby Redo Log Files:   Cleared
    chicago Online Redo Log Files:   Not Cleared
    chicago Standby Redo Log Files:  Available

-- ★ 검사 대상이 반대가 됐다.
--   이제 boston 의 정적 등록을 시험한다.
--   양쪽에 모두 등록해 둔 이유가 여기 있다.

--------------------------------------------------------------------------------
 [3] 새 Primary 에서 변경이 흘러가는가
--------------------------------------------------------------------------------

SYS@boston> INSERT INTO system.bk_t VALUES(95,'boston-primary');
SYS@boston> COMMIT;
SYS@boston> ALTER SYSTEM ARCHIVE LOG CURRENT;

System altered.

SYS@chicago> COL tag FOR a16
SYS@chicago> SELECT id, tag FROM system.bk_t ORDER BY id;

        ID TAG
---------- ----------------
         1 after-restart
        90 ship-off
        91 delayed
        92 listener-down
        93 traced
        94 before-swo
        95 boston-primary

7 rows selected.

-- ★ 94(전환 전)와 95(전환 후)가 모두 있다.
--   전환 전 데이터는 유지됐고 전환 후 데이터는 새 방향으로 흐른다.
--
-- ★ Switchover 는 무손실이다.
--   이것이 Failover 와의 가장 큰 차이이며 실습 07 에서 대비된다.

--------------------------------------------------------------------------------
 [4] 되돌린다
--------------------------------------------------------------------------------

SYS@chicago> SELECT db_unique_name, database_role, switchover_status FROM v$database;

DB_UNIQUE_ DATABASE_ROLE    SWITCHOVER_STATUS
---------- ---------------- ----------------------
chicago    PHYSICAL STANDBY NOT ALLOWED

1 row selected.

-- ★ 실습 01 과 같은 사전 점검을 다시 한다.
--   대상이 Standby 이고 NOT ALLOWED 인 것이 정상이다.

DGMGRL> switchover to 'chicago'

Performing switchover NOW, please wait...
Operation requires a connection to database "chicago"
Connecting ...
Connected to "chicago"
Connected as SYSDBA.
New primary database "chicago" is opening...
Operation requires start up of instance "boston" on database "boston"
Starting instance "boston"...
Connected to an idle instance.
ORACLE instance started.
Connected to "boston"
Database mounted.
Database opened.
Connected to "boston"
Switchover succeeded, new primary is "chicago"

-- ★ 실측 소요 약 69초. 명령이 대칭이다.
--
-- ★ 이번에는 boston 쪽에 Connected to an idle instance 가 나왔다.
--   내려갔다 올라오는 쪽이 바뀌었을 뿐 구조는 같다.

--------------------------------------------------------------------------------
 [5] 원상 확인
--------------------------------------------------------------------------------

SYS@chicago> SELECT db_unique_name, database_role, open_mode, switchover_status
  2            FROM v$database;

DB_UNIQUE_ DATABASE_ROLE    OPEN_MODE            SWITCHOVER_STATUS
---------- ---------------- -------------------- ----------------------
chicago    PRIMARY          READ WRITE           TO STANDBY

1 row selected.

SYS@boston> COL recovery_mode FOR a26
SYS@boston> COL name FOR a16
SYS@boston> COL value FOR a20
SYS@boston> SELECT db_unique_name, database_role, open_mode FROM v$database;
SYS@boston> SELECT process, status, sequence# FROM v$managed_standby WHERE process='MRP0';
SYS@boston> SELECT dest_id, status, recovery_mode FROM v$archive_dest_status WHERE dest_id=1;
SYS@boston> SELECT name, value FROM v$dataguard_stats WHERE name LIKE '%lag';

DB_UNIQUE_ DATABASE_ROLE    OPEN_MODE
---------- ---------------- --------------------
boston     PHYSICAL STANDBY READ ONLY WITH APPLY

PROCESS   STATUS         SEQUENCE#
--------- ------------ ----------
MRP0      APPLYING_LOG         90

   DEST_ID STATUS    RECOVERY_MODE
---------- --------- --------------------------
         1 VALID     MANAGED REAL TIME APPLY

NAME             VALUE
---------------- --------------------
transport lag    +00 00:00:00
apply lag        +00 00:00:00

-- ★ 실습 01 의 출발점으로 돌아왔다.
--   되돌리는 것까지가 훈련이다.

--------------------------------------------------------------------------------
 [6] 오류 재현 — 되돌린 뒤 옛 명령을 그대로 쓰면
--------------------------------------------------------------------------------

DGMGRL> switchover to 'chicago'      -- chicago 가 다시 Primary 인 상태에서

Performing switchover NOW, please wait...
Error: ORA-16558: database specified for switchover is not a standby database

Failed.
Unable to switchover, primary database is still "chicago"

>>> 원인 파악

-- ★ 전환 스크립트를 만들 때 가장 흔한 실수다.
--   "chicago 로 전환" 이라고 고정해 두면
--   이미 chicago 가 Primary 일 때 이 오류가 난다.
--
-- ★ 반대로 "boston 으로 전환" 이라고 고정하면
--   훈련 때마다 방향이 한쪽으로만 간다.

>>> 조치

-- ★ 역할을 먼저 판정하고 대상을 정한다.
--
--   현재 Primary 가 chicago 이면  → 대상은 boston
--   현재 Primary 가 boston  이면  → 대상은 chicago
--
--   SELECT db_unique_name FROM v$database WHERE database_role='PRIMARY';
--
-- ★ 15장에서 감시 스크립트에 대해 내린 결론과 같다.
--   역할을 먼저 판정하고 분기한다.

>>> 재확인

DGMGRL> show configuration

Configuration - dg_chicago

  Members:
  chicago - Primary database
    boston  - Physical standby database

Configuration Status:
SUCCESS

-- ★ 실패한 명령은 구성을 바꾸지 않았다.

--------------------------------------------------------------------------------
 [7] 정리
--------------------------------------------------------------------------------

-- ★ 이 실습에서 확인한 것
--
--   ① Broker 가 LOG_ARCHIVE_DEST_2 를 반대 방향으로 다시 쓴다
--   ② FAL_SERVER 는 비어 있다 — Broker 는 구성 정보를 쓴다
--   ③ LOG_ARCHIVE_CONFIG 에 10장의 farsync 가 남아 있다
--   ④ 구성 이름 dg_chicago 는 역할과 무관하다
--   ⑤ VALIDATE 의 정적 등록 검사 대상도 반대가 된다
--   ⑥ 전환 전후 데이터가 모두 유지된다 — Switchover 는 무손실
--   ⑦ 되돌리기도 같은 명령 한 줄, 소요 약 69초
--   ⑧ 대상을 고정한 스크립트는 ORA-16558 을 만난다
--
-- ★ 다음 실습에서 전환이 거부되는 경우를 모아 본다.
