# Standby 리스너 — 정적 등록이 반드시 필요하다.
SID_LIST_LISTENER =
  (SID_LIST =
    (SID_DESC =
      (GLOBAL_DBNAME = @STB_UNIQUE@)
      (ORACLE_HOME = @ORA_HOME@)
      (SID_NAME = @STB_SID@)
    )
    (SID_DESC =
      (GLOBAL_DBNAME = @STB_UNIQUE@_DGMGRL)
      (ORACLE_HOME = @ORA_HOME@)
      (SID_NAME = @STB_SID@)
    )
  )

LISTENER =
  (DESCRIPTION =
    (ADDRESS = (PROTOCOL = TCP)(HOST = @STB_HOST@.@DOMAIN@)(PORT = @PORT@))
  )

ADR_BASE_LISTENER = @ORA_BASE@
