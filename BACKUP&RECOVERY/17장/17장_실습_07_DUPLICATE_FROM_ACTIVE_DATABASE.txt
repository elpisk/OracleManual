================================================================================
 실습 17-7 : DUPLICATE TARGET DATABASE FROM ACTIVE DATABASE
 과정      : Oracle Database 19c Backup & Recovery
 환경      : Oracle 19.3.0 / Oracle Linux 7 / non-CDB ORCL → NEWDB / ARCHIVELOG
 소요 시간 : 약 35분
================================================================================

--------------------------------------------------------------------------------
[1] 시나리오 개요
--------------------------------------------------------------------------------
-- 원본에서 직접 읽어 복제본을 만든다.
-- Memory Script를 해석하며 진행 단계를 확인한다.
-- 17.2절의 수동 절차와 같은 구조임을 확인한다.
-- 완료 후 DBID와 경로를 검증한다.
-- 작업 유형 : 복제 수행(장애 복구 아님)

--------------------------------------------------------------------------------
[2] 사전 조건
--------------------------------------------------------------------------------
-- 실습 17-6이 완료되어 NEWDB가 NOMOUNT 상태여야 한다.

[oracle@oel7v9 ~]$ ps -ef | grep [p]mon_NEWDB
oracle   10442     1  0 11:32 ?        00:00:00 ora_pmon_NEWDB

[oracle@oel7v9 ~]$ sqlplus -s sys/oracle_4U@NEWDB as sysdba <<< \
> "SELECT instance_name, status FROM v\$instance;"

INSTANCE_NAME    STATUS
---------------- ------------
NEWDB            STARTED

--------------------------------------------------------------------------------
[3] 초기 상태 확인
--------------------------------------------------------------------------------
[oracle@oel7v9 ~]$ export ORACLE_SID=orcl
[oracle@oel7v9 ~]$ sqlplus / as sysdba

SYS@orcl> CREATE TABLE hr.dup177(id NUMBER, memo VARCHAR2(30)) TABLESPACE users;
SYS@orcl> INSERT INTO hr.dup177 VALUES (1, 'before duplicate');
SYS@orcl> COMMIT;

Commit complete.

SYS@orcl> SELECT dbid, name FROM v$database;

      DBID NAME
---------- ---------
1200530011 ORCL

SYS@orcl> SELECT file#, name FROM v$datafile ORDER BY file#;

     FILE# NAME
---------- ------------------------------------------
         1 /u03/oradata/ORCL/system01.dbf
         3 /u03/oradata/ORCL/sysaux01.dbf
         4 /u03/oradata/ORCL/undotbs01.dbf
         7 /u03/oradata/ORCL/users01.dbf
         8 /u03/oradata/ORCL/tbs13_01.dbf

SYS@orcl> SELECT name FROM v$controlfile;

NAME
------------------------------------------
/u01/app/oracle/oradata/orcl/control01.ctl
/u01/app/oracle/oradata/orcl/control02.ctl
/u02/oradata/orcl/control03.ctl

-- 원본의 컨트롤파일 경로를 적어 둔다. 바로 다음 단계에서 이 경로가 문제가 된다.

SYS@orcl> EXIT

[oracle@oel7v9 ~]$ df -h /u04 | tail -1
/dev/sdd1        30G  1.2G   29G   4% /u04

--------------------------------------------------------------------------------
[4] 작업 수행 : DUPLICATE 실행 → 오류 발생 → 원인 파악
--------------------------------------------------------------------------------
-- 이름과 경로만 SET 으로 지정해 실행해 본다.

[oracle@oel7v9 ~]$ rman target sys/oracle_4U@ORCL \
>   auxiliary sys/oracle_4U@NEWDB

Recovery Manager: Release 19.0.0.0.0 - Production

connected to target database: ORCL (DBID=1200530011)
connected to auxiliary database: NEWDB (not mounted)

RMAN> DUPLICATE TARGET DATABASE TO NEWDB
  2>   FROM ACTIVE DATABASE
  3>   SPFILE
  4>     SET DB_UNIQUE_NAME 'NEWDB'
  5>     SET DB_CREATE_FILE_DEST '/u04/newdb'
  6>     SET LOG_ARCHIVE_DEST_1 'LOCATION=/u04/newdb/arch'
  7>     SET SGA_TARGET '512M'
  8>     SET SGA_MAX_SIZE '512M';

Starting Duplicate Db at 19-MAY-25
using target database control file instead of recovery catalog
allocated channel: ORA_AUX_DISK_1
channel ORA_AUX_DISK_1: SID=11 device type=DISK
allocated channel: ORA_AUX_DISK_2
channel ORA_AUX_DISK_2: SID=150 device type=DISK
current log archived

contents of Memory Script:
{
   restore clone from service  'ORCL' spfile to
 '/u01/app/oracle/product/19.3.0/dbhome_1/dbs/spfileNEWDB.ora';
   sql clone "alter system set spfile= ''/u01/app/oracle/product/19.3.0/dbhome_1/dbs/spfileNEWDB.ora''";
}
executing Memory Script
...
contents of Memory Script:
{
   sql clone "alter system set  db_name =
 ''NEWDB'' comment=
 ''duplicate'' scope=spfile";
   sql clone "alter system set  db_unique_name =
 ''NEWDB'' comment=
 '''' scope=spfile";
   sql clone "alter system set  DB_CREATE_FILE_DEST =
 ''/u04/newdb'' comment=
 '''' scope=spfile";
   ...
   shutdown clone immediate;
   startup clone nomount;
}
executing Memory Script
...
Oracle instance shut down
connected to auxiliary database (not started)
Oracle instance started

Total System Global Area     536868920 bytes
...
duplicating Online logs to Oracle Managed File (OMF) location
duplicating Datafiles to Oracle Managed File (OMF) location

contents of Memory Script:
{
   sql clone "alter system set  db_name =
 ''ORCL'' comment=
 ''Modified by RMAN duplicate'' scope=spfile";
   sql clone "alter system set  db_unique_name =
 ''NEWDB'' comment=
 ''Modified by RMAN duplicate'' scope=spfile";
   shutdown clone immediate;
   startup clone force nomount
   restore clone from service  'ORCL' primary controlfile;
   alter clone database mount;
}
executing Memory Script
...
Starting restore at 19-MAY-25
channel ORA_AUX_DISK_1: starting datafile backup set restore
channel ORA_AUX_DISK_1: using network backup set from service ORCL
channel ORA_AUX_DISK_1: restoring control file
dbms_backup_restore.restoreCancel() failed
Oracle instance started
...
contents of Memory Script:
{
   sql clone "alter system set  db_name =
 ''NEWDB'' comment=
 ''Reset to original value by RMAN'' scope=spfile";
   sql clone "alter system reset  db_unique_name scope=spfile";
   shutdown clone immediate;
}
executing Memory Script
...
Oracle instance shut down
RMAN-00571: ===========================================================
RMAN-00569: =============== ERROR MESSAGE STACK FOLLOWS ===============
RMAN-00571: ===========================================================
RMAN-03002: failure of Duplicate Db command at 05/19/2025 11:44:41
RMAN-05501: aborting duplication of target database
RMAN-03015: error occurred in stored script Memory Script
ORA-19660: some files in the backup set could not be verified
ORA-19661: datafile 0 could not be verified
ORA-19849: error while reading backup piece from service ORCL
ORA-19504: failed to create file "/u01/app/oracle/oradata/orcl/control01.ctl"
ORA-27086: unable to lock file - already in use
Linux-x86_64 Error: 11: Resource temporarily unavailable
Additional information: 8
Additional information: 12792

-- << 원인 파악 >>
-- 컨트롤파일을 복원하는 단계에서 원본의 컨트롤파일 경로에 파일을 만들려 했다.
-- SPFILE 절은 원본의 spfile 을 통째로 복사한다. SET 으로 덮은 값 말고는 전부 원본 값이다.
-- control_files 도 그대로라 복제본이 '/u01/app/oracle/oradata/orcl/control01.ctl' 을 자기 것으로 안다.
-- 원본 인스턴스가 그 파일을 잠그고 있어(ORA-27086) 막혔을 뿐이다.
-- 원본이 내려가 있었다면 그 자리에 복제본 컨트롤파일을 써 버렸다. 17-2 의 리두 경로와 같은 함정이다.
-- db_create_file_dest 는 데이터파일·리두·임시 파일에만 적용된다. 컨트롤파일 경로는 control_files 가 정한다.
-- 같은 이유로 audit_file_dest 와 db_recovery_file_dest 도 원본 값을 물려받는다.
-- RMAN 은 실패하면서 spfile 의 db_name 을 되돌리고("Reset to original value") 인스턴스를 내린다.

-- << 수정 >> 원본을 가리킬 수 있는 파라미터를 전부 SET 으로 덮는다.
-- 복제본은 RMAN 이 내려 두었고 spfileNEWDB.ora 가 남아 있다. SPFILE 절은 auxiliary 가 pfile 로
-- 떠 있어야 하므로 spfile 을 지우고 pfile 로 다시 NOMOUNT 한다. 컨트롤파일 디렉터리도 미리 만든다.

RMAN> EXIT

[oracle@oel7v9 ~]$ rm -f $ORACLE_HOME/dbs/spfileNEWDB.ora
[oracle@oel7v9 ~]$ mkdir -p /u04/newdb/NEWDB
[oracle@oel7v9 ~]$ export ORACLE_SID=NEWDB
[oracle@oel7v9 ~]$ sqlplus -s / as sysdba <<< "STARTUP NOMOUNT PFILE='?/dbs/initNEWDB.ora'"

ORACLE instance started.
...

[oracle@oel7v9 ~]$ rman target sys/oracle_4U@ORCL auxiliary sys/oracle_4U@NEWDB

connected to target database: ORCL (DBID=1200530011)
connected to auxiliary database: NEWDB (not mounted)

RMAN> DUPLICATE TARGET DATABASE TO NEWDB
  2>   FROM ACTIVE DATABASE
  3>   SPFILE
  4>     SET DB_UNIQUE_NAME 'NEWDB'
  5>     SET CONTROL_FILES '/u04/newdb/NEWDB/control01.ctl'
  6>     SET DB_CREATE_FILE_DEST '/u04/newdb'
  7>     SET DB_RECOVERY_FILE_DEST ''
  8>     SET AUDIT_FILE_DEST '/u01/app/oracle/admin/NEWDB/adump'
  9>     SET LOG_ARCHIVE_DEST_1 'LOCATION=/u04/newdb/arch'
 10>     SET SGA_TARGET '512M'
 11>     SET SGA_MAX_SIZE '512M';

Starting Duplicate Db at 19-MAY-25
using target database control file instead of recovery catalog
allocated channel: ORA_AUX_DISK_1
allocated channel: ORA_AUX_DISK_2
current log archived

--------------------------------------------------------------------------------
[5] 진행 로그 : Memory Script 해석
--------------------------------------------------------------------------------
contents of Memory Script:
{
   restore clone from service  'ORCL' spfile to
 '/u01/app/oracle/product/19.3.0/dbhome_1/dbs/spfileNEWDB.ora';
}
executing Memory Script
...
channel ORA_AUX_DISK_1: restoring SPFILE
output file name=/u01/app/oracle/product/19.3.0/dbhome_1/dbs/spfileNEWDB.ora
Finished restore at 19-MAY-25

-- ① 원본 서비스에서 spfile 을 받아 SET 값을 적용하고 복제본을 재기동

contents of Memory Script:
{
   shutdown clone immediate;
   startup clone nomount;
}
executing Memory Script
Oracle instance shut down
connected to auxiliary database (not started)
Oracle instance started
...
duplicating Online logs to Oracle Managed File (OMF) location
duplicating Datafiles to Oracle Managed File (OMF) location

contents of Memory Script:
{
   shutdown clone immediate;
   startup clone force nomount
   restore clone from service  'ORCL' primary controlfile;
   alter clone database mount;
}
executing Memory Script

-- ② 컨트롤파일을 원본 서비스에서 직접 가져와 MOUNT

Starting restore at 19-MAY-25
channel ORA_AUX_DISK_1: restoring control file
output file name=/u04/newdb/NEWDB/control01.ctl
Finished restore at 19-MAY-25
database mounted

-- 이번에는 SET CONTROL_FILES 로 준 경로에 만들어졌다.

contents of Memory Script:
{
   set newname for clone datafile  1 to new;
   set newname for clone datafile  3 to new;
   set newname for clone datafile  4 to new;
   set newname for clone datafile  7 to new;
   set newname for clone datafile  8 to new;
   restore
   from  nonsparse   from service
 'ORCL'   clone database
   ;
   sql 'alter system archive log current';
}
executing Memory Script

-- ③ 경로를 새로 정하고 데이터파일을 가져온다.

Starting restore at 19-MAY-25
channel ORA_AUX_DISK_1: restoring datafile 00001 to /u04/newdb/NEWDB/datafile/o1_mf_system_%u_.dbf
channel ORA_AUX_DISK_2: restoring datafile 00003 to /u04/newdb/NEWDB/datafile/o1_mf_sysaux_%u_.dbf
...
Finished restore at 19-MAY-25
current log archived

contents of Memory Script:
{
   restore clone force from service  'ORCL'
           archivelog from scn  2792100;
   switch clone datafile all;
}
executing Memory Script

-- ④ 복사하는 동안 원본이 만든 아카이브(두 번 archive log current 한 것)를 가져오고
--    컨트롤파일의 경로를 새 위치로 갱신한다.

channel ORA_AUX_DISK_1: restoring archived log
archived log thread=1 sequence=16
channel ORA_AUX_DISK_2: restoring archived log
archived log thread=1 sequence=17
Finished restore at 19-MAY-25
datafile 1 switched to datafile copy
input datafile copy RECID=33 STAMP=1200622169 file name=/u04/newdb/NEWDB/datafile/o1_mf_system_obn6o5l0_.dbf
...

contents of Memory Script:
{
   set until scn  2792400;
   recover
   clone database
    delete archivelog
   ;
}
executing Memory Script

-- ⑤ 아카이브를 적용한다. 적용한 아카이브는 지운다.

Starting recover at 19-MAY-25
starting media recovery
archived log file name=/u04/newdb/arch/arch_1_16_1200606000.arc thread=1 sequence=16
archived log file name=/u04/newdb/arch/arch_1_17_1200606000.arc thread=1 sequence=17
media recovery complete, elapsed time: 00:00:00
Finished recover at 19-MAY-25

contents of Memory Script:
{
   delete clone force archivelog all;
}
executing Memory Script
...
Deleted 2 objects
Oracle instance started
...
sql statement: CREATE CONTROLFILE REUSE SET DATABASE "NEWDB" RESETLOGS ARCHIVELOG
  MAXLOGFILES     16
  MAXLOGMEMBERS      3
  MAXDATAFILES      100
  MAXINSTANCES     8
  MAXLOGHISTORY     2920
 LOGFILE
  GROUP     1  SIZE 50 M ,
  GROUP     3  SIZE 50 M ,
  GROUP     4  SIZE 50 M
 DATAFILE
  '/u04/newdb/NEWDB/datafile/o1_mf_system_obn6o5l0_.dbf'
 CHARACTER SET AL32UTF8

-- ⑥ 컨트롤파일을 SET DATABASE "NEWDB" 로 다시 만든다. 17-2·17-3 에서 손으로 한 일이다.
--    리두 경로가 없다. OMF 라 db_create_file_dest 아래에 자동으로 만들어진다.

contents of Memory Script:
{
   set newname for clone tempfile  1 to new;
   switch clone tempfile all;
   catalog clone datafilecopy  "/u04/newdb/NEWDB/datafile/o1_mf_sysaux_obn6pb7w_.dbf",
 ...
   switch clone datafile all;
}
executing Memory Script
renamed tempfile 1 to /u04/newdb/NEWDB/datafile/o1_mf_temp_%u_.tmp in control file
...
Reenabling controlfile options for auxiliary database
Executing: alter database enable block change tracking

-- ⑦ 임시 파일을 새 이름으로 등록하고(17-4 의 ADD TEMPFILE 에 해당), 원본에 켜져 있던
--    블록 변경 추적(14-4)을 복제본에도 켠다.

contents of Memory Script:
{
   Alter clone database open resetlogs;
}
executing Memory Script

database opened
Finished Duplicate Db at 19-MAY-25

-- 이 랩에서 1.75GB 기준 약 4분이 걸렸다.

--------------------------------------------------------------------------------
[6] 진단 : 결과 확인
--------------------------------------------------------------------------------
RMAN> EXIT

[oracle@oel7v9 ~]$ export ORACLE_SID=NEWDB
[oracle@oel7v9 ~]$ sqlplus / as sysdba

SYS@NEWDB> SELECT dbid, name, open_mode, log_mode FROM v$database;

      DBID NAME      OPEN_MODE            LOG_MODE
---------- --------- -------------------- ------------
1783204512 NEWDB     READ WRITE           ARCHIVELOG

-- DBID가 새로 부여되었다. 원본(1200530011)과 다르다.
-- 수동 복제(17-3)는 SET DATABASE 로 이름만 바뀌고 DBID 는 그대로였다.
-- DUPLICATE 도 안에서 같은 SET DATABASE 문을 쓰지만, 그 뒤에 DBID 까지 새로 매긴다.
-- nid 로 따로 바꿀 일이 없다.

SYS@NEWDB> SELECT file#, name FROM v$datafile ORDER BY file#;

     FILE# NAME
---------- --------------------------------------------------------
         1 /u04/newdb/NEWDB/datafile/o1_mf_system_obn6o5l0_.dbf
         3 /u04/newdb/NEWDB/datafile/o1_mf_sysaux_obn6pb7w_.dbf
         4 /u04/newdb/NEWDB/datafile/o1_mf_undotbs1_obn6prd6_.dbf
         7 /u04/newdb/NEWDB/datafile/o1_mf_users_obn6qrqy_.dbf
         8 /u04/newdb/NEWDB/datafile/o1_mf_tbs13_obn6rhh1_.dbf

-- OMF 이름으로 만들어졌다. 파일 번호는 원본과 같다.

SYS@NEWDB> SELECT COUNT(*) FROM v$tempfile;

  COUNT(*)
----------
         1

SYS@NEWDB> SELECT name FROM v$tempfile;

NAME
------------------------------------------------------------
/u04/newdb/NEWDB/datafile/o1_mf_temp_obn6t1gs_.tmp

-- 임시 파일도 자동으로 만들어졌다.
-- 수동 복제에서 직접 추가하던 단계가 사라졌다.

--------------------------------------------------------------------------------
[7] 오류 발생 → 원인 파악 → 수정
--------------------------------------------------------------------------------
-- 같은 명령을 한 번 더 실행해 본다.

SYS@NEWDB> EXIT
[oracle@oel7v9 ~]$ rman target sys/oracle_4U@ORCL auxiliary sys/oracle_4U@NEWDB

connected to target database: ORCL (DBID=1200530011)
connected to auxiliary database: NEWDB (DBID=1783204512)

RMAN> DUPLICATE TARGET DATABASE TO NEWDB FROM ACTIVE DATABASE;

Starting Duplicate Db at 19-MAY-25
using target database control file instead of recovery catalog
allocated channel: ORA_AUX_DISK_1
allocated channel: ORA_AUX_DISK_2
RMAN-00571: ===========================================================
RMAN-00569: =============== ERROR MESSAGE STACK FOLLOWS ===============
RMAN-00571: ===========================================================
RMAN-03002: failure of Duplicate Db command at 05/19/2025 12:02:11
RMAN-05501: aborting duplication of target database
RMAN-05500: the auxiliary database must be not mounted when issuing a DUPLICATE command

-- << 원인 파악 >>
-- 복제본이 이미 열려 있는 상태다. 접속 줄에 DBID 가 찍히는 것이 그 증거다(아까는 not mounted).
-- DUPLICATE는 auxiliary가 NOMOUNT 상태일 것을 요구한다.
-- 열린 복제본을 덮어쓰려면 명시적으로 내려야 한다.

RMAN> EXIT

-- << 수정 >> 복제본을 NOMOUNT로 내린 뒤 재수행한다.

[oracle@oel7v9 ~]$ export ORACLE_SID=NEWDB
[oracle@oel7v9 ~]$ sqlplus -s / as sysdba <<< \
> "SHUTDOWN IMMEDIATE
   STARTUP NOMOUNT;"

ORACLE instance shut down.
ORACLE instance started.

[oracle@oel7v9 ~]$ sqlplus -s sys/oracle_4U@NEWDB as sysdba <<< \
> "SELECT instance_name, status FROM v\$instance;"

INSTANCE_NAME    STATUS
---------------- ------------
NEWDB            STARTED

-- 이제 재수행할 수 있다. SPFILE 절을 다시 쓰려면 처음처럼 spfile 을 지우고 pfile 로 띄운다.
-- 실습에서는 이미 만들어진 복제본을 그대로 쓰므로 다시 연다.

[oracle@oel7v9 ~]$ sqlplus -s / as sysdba <<< "ALTER DATABASE MOUNT;
ALTER DATABASE OPEN;"

Database altered.
Database altered.

--------------------------------------------------------------------------------
[8] 검증 및 실무 포인트
--------------------------------------------------------------------------------
[oracle@oel7v9 ~]$ sqlplus / as sysdba

SYS@NEWDB> SELECT COUNT(*) FROM hr.dup177;

  COUNT(*)
----------
         1

SYS@NEWDB> SELECT memo FROM hr.dup177;

MEMO
------------------------------
before duplicate

-- 원본의 데이터가 그대로 복제되었다.

SYS@NEWDB> SELECT name, value FROM v$parameter
  2  WHERE  name IN ('log_archive_dest_1','db_create_file_dest','spfile',
  3                  'db_recovery_file_dest','sga_target','audit_file_dest');

NAME                     VALUE
------------------------ ----------------------------------------------------------------------
spfile                   /u01/app/oracle/product/19.3.0/dbhome_1/dbs/spfileNEWDB.ora
sga_target               536870912
log_archive_dest_1       LOCATION=/u04/newdb/arch
db_create_file_dest      /u04/newdb
db_recovery_file_dest
audit_file_dest          /u01/app/oracle/admin/NEWDB/adump

-- SET 절로 지정한 값이 모두 반영되었다. spfile 기반으로 동작한다. DUPLICATE가 만들어 주었다.
-- 첫 시도에서는 audit_file_dest 가 '/u01/app/oracle/admin/orcl/adump' 였다. 17-6 의 pfile 값이
-- 아니라 원본 spfile 값이다. SET 으로 덮지 않은 항목은 그렇게 남는다.

SYS@NEWDB> EXIT

[oracle@oel7v9 ~]$ du -sh /u04/newdb
1.9G    /u04/newdb

[oracle@oel7v9 ~]$ ls -R /u04/newdb | head -14
/u04/newdb:
arch
NEWDB

/u04/newdb/NEWDB:
changetracking
control01.ctl
datafile
onlinelog

-- OMF 는 종류별 디렉터리를 만든다. 블록 변경 추적 파일(changetracking)까지 따라왔다.

[oracle@oel7v9 ~]$ export ORACLE_SID=orcl
[oracle@oel7v9 ~]$ sqlplus -s / as sysdba <<< \
> "SELECT COUNT(*) FROM hr.dup177; SELECT dbid FROM v\$database;"

  COUNT(*)
----------
         1

      DBID
----------
1200530011

-- 원본은 그대로다.

-- 실무 포인트
-- 1) ACTIVE DUPLICATE는 명령 하나로 끝난다.
--    17.2절의 여러 단계가 Memory Script로 자동 실행된다.
--    수동 복제를 먼저 해 보면 이 로그가 훨씬 잘 읽힌다.
--
-- 2) SPFILE 절은 원본 spfile 의 복사다. SET 으로 덮지 않은 것은 전부 원본 값이다.
--    control_files 를 안 덮으면 원본 컨트롤파일 자리에 쓰려 든다(ORA-19504/ORA-27086).
--    원본이 떠 있어 잠겨 있었기에 막혔을 뿐이다. audit_file_dest, db_recovery_file_dest,
--    log_archive_dest_*, db_create_file_dest, sga 값을 한 번에 목록으로 덮는다.
--
-- 3) Memory Script는 일곱 구간으로 읽는다.
--    spfile 복원, 컨트롤파일 복원과 MOUNT, 데이터파일 복원, 아카이브 복원과 SWITCH,
--    복구, 컨트롤파일 재생성(SET DATABASE), 임시 파일·BCT 정리와 RESETLOGS 오픈이다.
--    clone 이 붙은 명령이 복제본을 대상으로 한다.
--
-- 4) DBID가 자동으로 새로 부여된다.
--    수동 복제의 SET DATABASE 는 이름만 바꾸지만 DUPLICATE 는 DBID 까지 새로 만든다.
--    복제 후 반드시 확인한다.
--
-- 5) db_create_file_dest 를 지정하면 데이터파일·리두·임시 파일이 OMF 이름으로 만들어진다.
--    경로 변환 규칙이 필요 없어 실수 여지가 줄어든다. 컨트롤파일만은 SET CONTROL_FILES 로 따로 준다.
--    파일명이 알아보기 어려워지므로 운영 환경에서는 판단이 필요하다.
--
-- 6) 복제본이 열려 있으면 DUPLICATE를 다시 할 수 없다.
--    RMAN-05500 으로 거부된다. NOMOUNT로 내린 뒤 재수행해야 한다.
--    실패한 DUPLICATE 가 남긴 spfileNEWDB.ora 는 SPFILE 절 재수행 전에 지운다.
