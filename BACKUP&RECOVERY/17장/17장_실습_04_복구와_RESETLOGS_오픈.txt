================================================================================
 실습 17-4 : 복구와 RESETLOGS 오픈, TEMP 파일 추가
 과정      : Oracle Database 19c Backup & Recovery
 환경      : Oracle 19.3.0 / Oracle Linux 7 / non-CDB ORCL → CLONE / ARCHIVELOG
 소요 시간 : 약 25분
================================================================================

--------------------------------------------------------------------------------
[1] 시나리오 개요
--------------------------------------------------------------------------------
-- 컨트롤파일을 만든 복제본을 복구해 연다.
-- UNTIL CANCEL 로 아카이브를 적용한다.
-- RESETLOGS로 열고 임시 파일을 새로 만든다.
-- 10장의 불완전 복구 절차와 같다는 점을 확인한다.
-- 작업 유형 : 복제 수행(장애 복구 아님)

--------------------------------------------------------------------------------
[2] 사전 조건
--------------------------------------------------------------------------------
-- 실습 17-3이 완료되어 복제본이 MOUNT 상태여야 한다.

[oracle@oel7v9 ~]$ export ORACLE_SID=CLONE
[oracle@oel7v9 ~]$ sqlplus / as sysdba

SYS@CLONE> SELECT status FROM v$instance;

STATUS
------------
MOUNTED

SYS@CLONE> SELECT dbid, name FROM v$database;

      DBID NAME
---------- ---------
1200530011 CLONE

--------------------------------------------------------------------------------
[3] 초기 상태 확인
--------------------------------------------------------------------------------
SYS@CLONE> SELECT file#, checkpoint_change#, fuzzy FROM v$datafile_header
  2  ORDER BY file#;

     FILE# CHECKPOINT_CHANGE# FUZ
---------- ------------------ ---
         1            2760100 NO
         3            2760100 NO
         4            2760100 NO
         7            2760100 NO
         8            2760100 NO

-- 모든 파일이 같은 SCN이고 FUZZY가 NO다.
-- Cold Backup에서 복사했기 때문이며 일관성이 있는 상태다.

SYS@CLONE> !ls /u04/clone/arch/*.arc | head -3
/u04/clone/arch/arch_1_12_1200606000.arc
/u04/clone/arch/arch_1_13_1200606000.arc
/u04/clone/arch/arch_1_14_1200606000.arc

SYS@CLONE> !ls /u04/clone/arch/*.arc | wc -l
3

-- 백업 이후에 원본이 만든 아카이브 셋이 준비되어 있다.

--------------------------------------------------------------------------------
[4] 작업 수행 : 복구 시도
--------------------------------------------------------------------------------
-- 일반 복구를 먼저 시도해 본다.

SYS@CLONE> RECOVER DATABASE

ORA-00283: recovery session canceled due to errors
ORA-01610: recovery using the BACKUP CONTROLFILE option must be done

-- << 원인 파악 >>
-- 새로 만든 컨트롤파일은 데이터파일의 최신 SCN 정보를 갖고 있지 않다.
-- 어디까지 복구해야 하는지 판단할 근거가 없다.
-- USING BACKUP CONTROLFILE 로 그 판단을 사람에게 넘기라는 뜻이다.
-- 6장과 11장에서 컨트롤파일을 재생성한 뒤 만난 것과 같은 오류다.

-- << 수정 >>

SYS@CLONE> SET AUTORECOVERY ON
SYS@CLONE> RECOVER DATABASE USING BACKUP CONTROLFILE UNTIL CANCEL

ORA-00279: change 2760100 generated at 05/19/2025 09:12:04 needed for thread 1
ORA-00289: suggestion : /u04/clone/arch/arch_1_12_1200606000.arc
ORA-00280: change 2760100 for thread 1 is in sequence #12
ORA-00279: change 2761300 generated at 05/19/2025 09:18:02 needed for thread 1
ORA-00289: suggestion : /u04/clone/arch/arch_1_13_1200606000.arc
ORA-00280: change 2761300 for thread 1 is in sequence #13
ORA-00278: log file '/u04/clone/arch/arch_1_12_1200606000.arc' no longer
needed for this recovery
ORA-00279: change 2761306 generated at 05/19/2025 09:18:02 needed for thread 1
ORA-00289: suggestion : /u04/clone/arch/arch_1_14_1200606000.arc
ORA-00280: change 2761306 for thread 1 is in sequence #14
ORA-00278: log file '/u04/clone/arch/arch_1_13_1200606000.arc' no longer
needed for this recovery
ORA-00279: change 2761325 generated at 05/19/2025 09:18:04 needed for thread 1
ORA-00289: suggestion : /u04/clone/arch/arch_1_15_1200606000.arc
ORA-00280: change 2761325 for thread 1 is in sequence #15
ORA-00278: log file '/u04/clone/arch/arch_1_14_1200606000.arc' no longer
needed for this recovery
ORA-00308: cannot open archived log '/u04/clone/arch/arch_1_15_1200606000.arc'
ORA-27037: unable to obtain file status
Linux-x86_64 Error: 2: No such file or directory
Additional information: 7

-- AUTORECOVERY 에서는 "Log applied." 대신 ORA-00278(이 로그는 더 필요 없다)로 적용이 끝났음을 알린다.
-- 12, 13, 14번이 차례로 적용됐고 15번을 요구한다.
-- 15번은 원본의 CURRENT 리두에 있어 여기에 없다. 여기까지가 한계다.

--------------------------------------------------------------------------------
[5] 진행 로그 : CANCEL과 오픈
--------------------------------------------------------------------------------
SYS@CLONE> SET AUTORECOVERY OFF
SYS@CLONE> RECOVER DATABASE USING BACKUP CONTROLFILE UNTIL CANCEL

ORA-00279: change 2761325 generated at 05/19/2025 09:18:04 needed for thread 1
ORA-00289: suggestion : /u04/clone/arch/arch_1_15_1200606000.arc
ORA-00280: change 2761325 for thread 1 is in sequence #15

Specify log: {<RET>=suggested | filename | AUTO | CANCEL}
CANCEL

Media recovery cancelled.

SYS@CLONE> ALTER DATABASE OPEN;
ALTER DATABASE OPEN
*
ERROR at line 1:
ORA-01589: must use RESETLOGS or NORESETLOGS option for database open

-- << 원인 파악 >>
-- 백업 컨트롤파일 방식으로 복구했으므로 RESETLOGS가 강제된다.
-- 리두 파일도 아직 없으므로 새로 만들어야 한다.
-- 10장의 불완전 복구와 완전히 같은 흐름이다.

SYS@CLONE> ALTER DATABASE OPEN RESETLOGS;

Database altered.

--------------------------------------------------------------------------------
[6] 진행 로그 : 임시 파일 추가
--------------------------------------------------------------------------------
SYS@CLONE> SELECT COUNT(*) FROM v$tempfile;

  COUNT(*)
----------
         0

-- 임시 파일이 없다. 복사하지 않았기 때문이다.

SYS@CLONE> SELECT tablespace_name, status FROM dba_tablespaces
  2  WHERE  contents = 'TEMPORARY';

TABLESPACE_NAME                STATUS
------------------------------ ---------
TEMP                           ONLINE

-- 테이블스페이스는 정의되어 있으나 파일이 없다.
-- 정렬이 메모리를 넘어 디스크로 흘러야 드러난다. 큰 정렬을 일부러 만든다.

SYS@CLONE> SELECT MAX(rn) FROM (
  2    SELECT ROW_NUMBER() OVER (ORDER BY a.object_name, b.n) rn
  3    FROM   dba_objects a, (SELECT LEVEL n FROM dual CONNECT BY LEVEL <= 30) b);
SELECT MAX(rn) FROM (SELECT ROW_NUMBER() OVER (ORDER BY a.object_name, b.n) rn FROM dba_objects a, (SELECT LEVEL n FROM dual CONNECT BY LEVEL <= 30) b)
                                                                                    *
ERROR at line 1:
ORA-25153: Temporary Tablespace is Empty

-- 220만 행 정렬이 임시 영역으로 흘러넘치는 순간 ORA-25153 이 난다. "파일이 없다" 는 뜻이다.
-- 작은 정렬은 메모리에서 끝나 오류가 없다. 다음 조회는 그대로 된다.

SYS@CLONE> SELECT object_name FROM dba_objects ORDER BY object_name DESC FETCH FIRST 1 ROWS ONLY;

OBJECT_NAME
--------------------------------------------------------------------------------
sun/util/xml/PlatformXmlPropertiesProvider$Resolver

-- 그래서 복제 직후 몇 번 조회해 보고 "된다" 고 넘어가면 나중에 배치에서 터진다.

SYS@CLONE> ALTER TABLESPACE temp ADD TEMPFILE
  2    '/u04/clone/CLONE/temp01.dbf' SIZE 100M;

Tablespace altered.

SYS@CLONE> SELECT name, bytes/1024/1024 AS mb FROM v$tempfile;

NAME                                       MB
------------------------------------------- ----------
/u04/clone/CLONE/temp01.dbf                       100

SYS@CLONE> SELECT MAX(rn) FROM (
  2    SELECT ROW_NUMBER() OVER (ORDER BY a.object_name, b.n) rn
  3    FROM   dba_objects a, (SELECT LEVEL n FROM dual CONNECT BY LEVEL <= 30) b);

   MAX(RN)
----------
   2203170

-- 같은 정렬이 통과한다.

--------------------------------------------------------------------------------
[7] 오류 발생 → 원인 파악 → 수정
--------------------------------------------------------------------------------
-- 아카이브가 어디에 만들어지는지 확인한다.

SYS@CLONE> ALTER SYSTEM SWITCH LOGFILE;

System altered.

SYS@CLONE> !ls -lt /u04/clone/arch/*.arc | head -2
-rw-r-----. 1 oracle dba   528384 May 19 10:41 /u04/clone/arch/arch_1_1_1200621700.arc
-rw-r-----. 1 oracle dba    17408 May 19 09:18 /u04/clone/arch/arch_1_14_1200606000.arc

-- 복제본의 아카이브가 복제본 경로에 만들어졌다.
-- 시퀀스가 1부터 다시 시작하고 리셋 스탬프(1200621700)도 새것이다. RESETLOGS로 열었기 때문이다.

SYS@CLONE> !ls -lt /home/oracle/arch1/*.arc | head -2
-rw-r-----. 1 oracle dba    17408 May 19 09:18 /home/oracle/arch1/arch_1_14_1200606000.arc
-rw-r-----. 1 oracle dba   563200 May 19 09:18 /home/oracle/arch1/arch_1_12_1200606000.arc

-- 원본 경로에는 복제본의 아카이브가 섞이지 않았다.
-- log_archive_dest_1 을 제대로 바꾼 결과다.

-- 만약 이 값을 바꾸지 않았다면 어떻게 되는지 확인해 본다.

SYS@CLONE> ALTER SYSTEM SET log_archive_dest_1='LOCATION=/home/oracle/arch1' SCOPE=MEMORY;

System altered.

SYS@CLONE> ALTER SYSTEM SWITCH LOGFILE;

System altered.

SYS@CLONE> !ls -lt /home/oracle/arch1/*.arc | head -2
-rw-r-----. 1 oracle dba  1378304 May 19 10:44 /home/oracle/arch1/arch_1_2_1200621700.arc
-rw-r-----. 1 oracle dba    17408 May 19 09:18 /home/oracle/arch1/arch_1_14_1200606000.arc

-- << 원인 파악 >>
-- 복제본의 아카이브(시퀀스 2)가 원본 디렉터리에 만들어졌다.
-- 파일명의 리셋 스탬프가 달라 구분은 되지만 섞여 있다.
-- 원본에서 아카이브 정리 스크립트를 돌리면 판단이 어려워진다.
-- 반대로 복제본에서 정리하면 원본 아카이브를 지울 위험이 있다.
-- DBID 까지 같으므로(17-3) 원본이 RMAN 으로 이 디렉터리를 CATALOG 하면 남의 로그를 자기 것으로 등록한다.

-- << 수정 >>

SYS@CLONE> ALTER SYSTEM SET log_archive_dest_1='LOCATION=/u04/clone/arch'
  2    SCOPE=MEMORY;

System altered.

-- pfile 로 기동한 인스턴스라 MEMORY 로만 되돌린다. pfile 의 값은 처음부터 맞게 적어 두었다.

SYS@CLONE> !rm -f /home/oracle/arch1/arch_1_2_1200621700.arc

-- 섞인 파일을 지운다. 리셋 스탬프로 복제본 것을 구분했다.

SYS@CLONE> ALTER SYSTEM SWITCH LOGFILE;
SYS@CLONE> !ls -lt /u04/clone/arch/*.arc | head -1
-rw-r-----. 1 oracle dba  2864640 May 19 10:47 /u04/clone/arch/arch_1_3_1200621700.arc

--------------------------------------------------------------------------------
[8] 검증 및 실무 포인트
--------------------------------------------------------------------------------
SYS@CLONE> SELECT dbid, name, open_mode, log_mode FROM v$database;

      DBID NAME      OPEN_MODE            LOG_MODE
---------- --------- -------------------- ------------
1200530011 CLONE     READ WRITE           ARCHIVELOG

SYS@CLONE> SELECT COUNT(*) FROM hr.employees;

  COUNT(*)
----------
       107

SYS@CLONE> SELECT * FROM hr.ch17_after ORDER BY id;

        ID MEMO
---------- ----------------------------------------
         1 after cold backup
         2 after switch 1

-- 원본의 데이터가 그대로 있다. 백업 뒤에 넣은 두 행까지 있다.
-- 아카이브 12~14번을 적용한 결과다. 콜드 백업만 붙였다면 이 표 자체가 없다.

SYS@CLONE> SELECT * FROM v$recover_file;

no rows selected

SYS@CLONE> SELECT name FROM v$datafile
  2  UNION ALL SELECT name FROM v$tempfile
  3  UNION ALL SELECT member FROM v$logfile
  4  UNION ALL SELECT name FROM v$controlfile;

NAME
------------------------------------------
/u04/clone/CLONE/system01.dbf
/u04/clone/CLONE/sysaux01.dbf
/u04/clone/CLONE/undotbs01.dbf
/u04/clone/CLONE/users01.dbf
/u04/clone/CLONE/tbs13_01.dbf
/u04/clone/CLONE/temp01.dbf
/u04/clone/CLONE/redo04.log
...
/u04/clone/CLONE/control01.ctl

13 rows selected.

-- 모든 파일이 복제본 경로에 있다.

SYS@CLONE> SELECT COUNT(*) FROM dba_objects WHERE status = 'INVALID';

  COUNT(*)
----------
         9

-- 원본에도 있던 무효 객체다(원본에서 같은 조회를 해 보면 같은 수가 나온다). 복제 때문에 생긴 것이 아니다.

SYS@CLONE> SELECT resetlogs_change#, resetlogs_time FROM v$database;

RESETLOGS_CHANGE# RESETLOGS_TIME
----------------- --------------------
          2761326 19-MAY-2025 10:38:11

-- 마지막으로 적용한 SCN(2761325) 바로 다음이 새 인카네이션의 시작이다.

-- 실무 포인트
-- 1) 재생성한 컨트롤파일로 일반 RECOVER를 시도하면 ORA-01610이 나온다.
--    복구 한계를 판단할 근거가 컨트롤파일에 없기 때문이다.
--    USING BACKUP CONTROLFILE 로 그 판단을 사람이 맡는다.
--
-- 2) 마지막 아카이브까지 적용되면 ORA-00308이 나온다.
--    오류가 아니라 도달 가능한 한계에 이르렀다는 신호다.
--    CANCEL로 마무리하면 된다. 10장과 같은 흐름이다.
--
-- 3) RESETLOGS로 열어야 하며 리두 파일이 그때 만들어진다.
--    복제본의 리두 시퀀스가 1부터 시작하는 것을 확인할 수 있다.
--
-- 4) 임시 파일은 반드시 새로 만든다.
--    없으면 디스크로 흘러넘치는 정렬에서 ORA-25153 이 발생한다.
--    작은 조회는 메모리에서 끝나 멀쩡하므로 "몇 번 해 봤더니 됐다" 는 검증이 아니다.
--    복제 절차의 마지막 단계로 넣어 둔다.
--
-- 5) 아카이브 경로를 반드시 확인한다.
--    복제본의 아카이브가 원본 디렉터리에 섞이면 정리 작업이 위험해진다.
--    리셋 스탬프로 구분은 되지만 실수 여지가 크고, DBID 가 같은 지금은 RMAN 도 구분하지 못한다.
--
-- 6) 복제본이 열리면 파일 목록을 전수 조회한다.
--    v$datafile, v$tempfile, v$logfile, v$controlfile 네 뷰를 모두 본다.
--    하나라도 원본 경로가 있으면 즉시 조치한다.
