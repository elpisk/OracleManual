================================================================================
 실습 19-8 : NETWORK_LINK 직접 이관과 VERSION 파라미터
 과정      : Oracle Database 19c Backup & Recovery
 환경      : Oracle 19.3.0 / Oracle Linux 7 / non-CDB ORCL / ARCHIVELOG
 소요 시간 : 약 35분
================================================================================

--------------------------------------------------------------------------------
[1] 실습 개요
--------------------------------------------------------------------------------
-- 덤프 파일 없이 데이터베이스 링크로 직접 반입한다.
-- 링크 생성과 tnsnames 설정, 권한 조건을 확인한다.
-- VERSION 파라미터의 방향과 미지정 시의 오류를 확인한다.
-- NETWORK_LINK 방식의 제약도 함께 본다.
-- 작업 유형 : 스키마 이관

--------------------------------------------------------------------------------
[2] 사전 조건
--------------------------------------------------------------------------------
-- 같은 서버 안에서 자기 자신을 가리키는 링크로 동작을 확인한다.
-- 실제 환경에서는 원격 데이터베이스를 가리킨다.

[oracle@oel7v9 ~]$ lsnrctl status | head -14

LSNRCTL for Linux: Version 19.0.0.0.0 - Production on 21-MAY-2025 17:00:10

Copyright (c) 1991, 2019, Oracle.  All rights reserved.

Connecting to (DESCRIPTION=(ADDRESS=(PROTOCOL=TCP)(HOST=oel7v9)(PORT=1521)))
STATUS of the LISTENER
------------------------
Alias                     LISTENER
Version                   TNSLSNR for Linux: Version 19.0.0.0.0 - Production
Start Date                21-MAY-2025 08:30:11
Uptime                    0 days 8 hr. 29 min. 59 sec
Trace Level               off
Security                  ON: Local OS Authentication

[oracle@oel7v9 ~]$ grep -A9 '^ORCL' $ORACLE_HOME/network/admin/tnsnames.ora

ORCL =
  (DESCRIPTION =
    (ADDRESS_LIST =
      (ADDRESS = (PROTOCOL = TCP)(HOST = oel7v9)(PORT = 1521))
    )
    (CONNECT_DATA =
      (SERVER = DEDICATED)
      (SERVICE_NAME = orcl)
    )
  )

[oracle@oel7v9 ~]$ tnsping ORCL | tail -2

Attempting to contact (DESCRIPTION = (ADDRESS_LIST = (ADDRESS = (PROTOCOL = TCP)(HOST = oel7v9)(PORT = 1521))) (CONNECT_DATA = (SERVER = DEDICATED) (SERVICE_NAME = orcl)))
OK (30 msec)

--------------------------------------------------------------------------------
[3] 초기 상태 확인
--------------------------------------------------------------------------------
[oracle@oel7v9 ~]$ sqlplus / as sysdba

SYS@orcl> CREATE USER net19 IDENTIFIED BY oracle_4U
  2    DEFAULT TABLESPACE users QUOTA UNLIMITED ON users;
SYS@orcl> GRANT CREATE SESSION, RESOURCE TO net19;

SYS@orcl> CREATE TABLE net19.src TABLESPACE users AS
  2    SELECT LEVEL AS id, RPAD('n-'||LEVEL, 60, 'z') AS memo
  3    FROM dual CONNECT BY LEVEL <= 20000;

Table created.

SYS@orcl> CREATE INDEX net19.idx_src ON net19.src(id) TABLESPACE users;
SYS@orcl> EXEC DBMS_STATS.gather_schema_stats('NET19');

PL/SQL procedure successfully completed.

SYS@orcl> SELECT COUNT(*) FROM net19.src;

  COUNT(*)
----------
     20000

SYS@orcl> CREATE USER net19b IDENTIFIED BY oracle_4U
  2    DEFAULT TABLESPACE users QUOTA UNLIMITED ON users;
SYS@orcl> GRANT CREATE SESSION, RESOURCE TO net19b;

User created.

--------------------------------------------------------------------------------
[4] 작업 수행 : 데이터베이스 링크 생성
--------------------------------------------------------------------------------
SYS@orcl> CREATE PUBLIC DATABASE LINK src_link
  2    CONNECT TO system IDENTIFIED BY oracle_4U USING 'ORCL';

Database link created.

SYS@orcl> SELECT SYSDATE FROM dual@src_link;

SYSDATE
---------
21-MAY-25

SYS@orcl> SELECT db_link, host, username FROM dba_db_links
  2  WHERE  db_link = 'SRC_LINK';

DB_LINK    HOST   USERNAME
---------- ------ ---------
SRC_LINK   ORCL   SYSTEM

-- 링크가 정상 동작한다.

--------------------------------------------------------------------------------
[5] 진행 로그 : NETWORK_LINK 반입
--------------------------------------------------------------------------------
[oracle@oel7v9 ~]$ impdp system/oracle_4U directory=dp_dir \
>   network_link=src_link schemas=net19 \
>   remap_schema=net19:net19b logfile=net_imp.log

Import: Release 19.0.0.0.0 - Production on Wed May 21 17:04:22 2025

Starting "SYSTEM"."SYS_IMPORT_SCHEMA_01":  system/******** directory=dp_dir network_link=src_link schemas=net19 remap_schema=net19:net19b logfile=net_imp.log
Estimate in progress using BLOCKS method...
Processing object type SCHEMA_EXPORT/TABLE/TABLE_DATA
Total estimation using BLOCKS method: 2 MB
Processing object type SCHEMA_EXPORT/USER
ORA-31684: Object type USER:"NET19B" already exists
Processing object type SCHEMA_EXPORT/SYSTEM_GRANT
Processing object type SCHEMA_EXPORT/ROLE_GRANT
Processing object type SCHEMA_EXPORT/DEFAULT_ROLE
Processing object type SCHEMA_EXPORT/TABLESPACE_QUOTA
Processing object type SCHEMA_EXPORT/PRE_SCHEMA/PROCACT_SCHEMA
Processing object type SCHEMA_EXPORT/TABLE/TABLE
. . imported "NET19B"."SRC"                               20000 rows
Processing object type SCHEMA_EXPORT/TABLE/INDEX/INDEX
Processing object type SCHEMA_EXPORT/TABLE/INDEX/STATISTICS/INDEX_STATISTICS
Processing object type SCHEMA_EXPORT/TABLE/STATISTICS/TABLE_STATISTICS
Processing object type SCHEMA_EXPORT/STATISTICS/MARKER
Job "SYSTEM"."SYS_IMPORT_SCHEMA_01" completed with 1 error(s) at Wed May 21 17:04:43 2025 elapsed 0 00:00:21

-- 반입인데 "Estimate in progress" 가 먼저 나온다. 원본에서 직접 읽으므로 반출 단계가 안에 들어 있다.
-- . . imported 줄에 크기가 없다. 덤프 파일을 거치지 않아 파일 크기를 잴 것이 없다.

SYS@orcl> SELECT COUNT(*) FROM net19b.src;

  COUNT(*)
----------
     20000

-- 덤프 파일 없이 옮겨졌다.

[oracle@oel7v9 ~]$ ls -lt /u03/dpdump/*.dmp | head -3

-rw-r-----. 1 oracle dba 184320 May 21 13:25 /u03/dpdump/tb_t1.dmp
-rw-r-----. 1 oracle dba 372736 May 21 13:24 /u03/dpdump/sc_app1.dmp
-rw-r-----. 1 oracle dba 204800 May 21 13:22 /u03/dpdump/ts_dpts.dmp

-- 새로 만들어진 덤프 파일이 없다(가장 최근 것이 19-4 의 파일이다). 로그만 생성되었다.

--------------------------------------------------------------------------------
[6] 확인 : VERSION 파라미터
--------------------------------------------------------------------------------
SYS@orcl> SELECT name, value FROM v$parameter WHERE name = 'compatible';

NAME        VALUE
----------- --------
compatible  19.0.0

[oracle@oel7v9 ~]$ expdp system/oracle_4U directory=dp_dir \
>   dumpfile=v121.dmp logfile=v121.log schemas=net19 version=12.1

Warning: Oracle Data Pump is exporting from a database that supports long identifiers to a version that does not support long identifiers.
Starting "SYSTEM"."SYS_EXPORT_SCHEMA_01":  system/******** directory=dp_dir dumpfile=v121.dmp logfile=v121.log schemas=net19 version=12.1
...
. . exported "NET19"."SRC"                               1.330 MB   20000 rows
Job "SYSTEM"."SYS_EXPORT_SCHEMA_01" successfully completed at Wed May 21 17:10:33 2025 elapsed 0 00:00:19

-- 경고가 하나 붙는다. 12.2 부터 허용된 128자 식별자를 12.1 은 못 받는다는 뜻이다.
-- 이 스키마에는 긴 이름이 없어 실제로 빠진 것은 없다.

[oracle@oel7v9 ~]$ expdp system/oracle_4U directory=dp_dir \
>   dumpfile=v19.dmp logfile=v19.log schemas=net19

. . exported "NET19"."SRC"                               1.330 MB   20000 rows
Job "SYSTEM"."SYS_EXPORT_SCHEMA_01" successfully completed at Wed May 21 17:11:20 2025 elapsed 0 00:00:19

-- 두 덤프의 내부 버전을 헤더에서 읽어 본다.

SYS@orcl> SET SERVEROUTPUT ON
SYS@orcl> DECLARE
  2    t  ku$_dumpfile_info;
  3    ft NUMBER;
  4  BEGIN
  5    FOR f IN (SELECT 'v121.dmp' n FROM dual UNION ALL SELECT 'v19.dmp' FROM dual) LOOP
  6      DBMS_DATAPUMP.get_dumpfile_info(f.n, 'DP_DIR', t, ft);
  7      FOR i IN 1..t.COUNT LOOP
  8        IF t(i).item_code IN (1, 15) THEN
  9          DBMS_OUTPUT.put_line(f.n || '  item ' || t(i).item_code || ' = ' || t(i).value);
 10        END IF;
 11      END LOOP;
 12    END LOOP;
 13  END;
 14  /

v121.dmp  item 1 = 4.1
v121.dmp  item 15 = 12.01.00.00.00
v19.dmp  item 1 = 5.1
v19.dmp  item 15 = 19.00.00.00.00

PL/SQL procedure successfully completed.

-- item 1 은 덤프 파일 형식 버전, item 15 는 그 형식을 쓰는 DB 버전이다.
-- VERSION=12.1 로 만든 덤프는 4.1 형식, 기본은 5.1 형식이다. 19c에서는 둘 다 읽을 수 있다.

[oracle@oel7v9 ~]$ impdp system/oracle_4U directory=dp_dir \
>   dumpfile=v121.dmp schemas=net19 remap_schema=net19:net19b \
>   table_exists_action=truncate logfile=v121_imp.log

...
Table "NET19B"."SRC" exists and has been truncated. Data will be loaded but all dependent metadata will be skipped due to table_exists_action of truncate
Processing object type SCHEMA_EXPORT/TABLE/TABLE_DATA
. . imported "NET19B"."SRC"                              1.330 MB   20000 rows
...
Job "SYSTEM"."SYS_IMPORT_SCHEMA_01" completed with 1 error(s) at Wed May 21 17:12:44 2025 elapsed 0 00:00:04

-- 1건은 ORA-31684(사용자 있음)다.

--------------------------------------------------------------------------------
[7] 오류 발생 → 원인 파악 → 수정
--------------------------------------------------------------------------------
-- NETWORK_LINK에 덤프 파일을 함께 지정해 본다.

[oracle@oel7v9 ~]$ impdp system/oracle_4U directory=dp_dir \
>   network_link=src_link dumpfile=net.dmp schemas=net19 \
>   remap_schema=net19:net19b logfile=net2.log

Import: Release 19.0.0.0.0 - Production on Wed May 21 17:15:02 2025

Connected to: Oracle Database 19c Enterprise Edition Release 19.0.0.0.0 - Production
UDI-00011: parameter dumpfile is incompatible with parameter network_link

-- << 원인 파악 >>
-- NETWORK_LINK는 원본에서 읽어 대상에 바로 넣는 방식이다.
-- 중간 파일을 만들지 않으므로 DUMPFILE이라는 개념 자체가 없다.
-- 두 파라미터는 서로 다른 동작 방식을 지시하므로 함께 쓸 수 없다.
-- 19-2 의 INCLUDE/EXCLUDE 처럼 클라이언트(impdp)가 파라미터 단계에서 UDI-00011 로 거부한다.
-- 작업(Job)은 만들어지지 않는다.
-- 덤프 파일이 필요하면 NETWORK_LINK를 쓰지 않고 expdp로 만든다.

-- << 수정 >> DUMPFILE을 뺀다.

[oracle@oel7v9 ~]$ impdp system/oracle_4U directory=dp_dir \
>   network_link=src_link schemas=net19 \
>   remap_schema=net19:net19b table_exists_action=truncate \
>   logfile=net2.log

...
Table "NET19B"."SRC" exists and has been truncated. Data will be loaded but all dependent metadata will be skipped due to table_exists_action of truncate
. . imported "NET19B"."SRC"                               20000 rows
...
Job "SYSTEM"."SYS_IMPORT_SCHEMA_01" completed with 1 error(s) at Wed May 21 17:17:30 2025 elapsed 0 00:00:12

-- 상위 버전 덤프를 하위 버전에서 읽는 상황도 알아 둔다. 이 랩에는 12.1 이 없어 문서 기준이다.
-- 12.1 데이터베이스에서 v19.dmp(5.1 형식)를 반입하면 다음과 같이 거부된다.
-- (12.2 부터는 덤프 형식이 19c 와 같은 5.1 이라 그대로 읽힌다)

[oracle@target121 ~]$ impdp system/oracle_4U directory=dp_dir \
>   dumpfile=v19.dmp schemas=net19

ORA-39001: invalid argument value
ORA-39000: bad dump file specification
ORA-39142: incompatible version number 5.1 in dump file
"/u03/dpdump/v19.dmp"

-- 이 경우 대상에서 할 수 있는 일이 없다.
-- 원본에서 version=12.1로 다시 내보내야 한다.

--------------------------------------------------------------------------------
[8] 검증 및 실무 포인트
--------------------------------------------------------------------------------
SYS@orcl> SELECT COUNT(*) FROM net19b.src;

  COUNT(*)
----------
     20000

SYS@orcl> SELECT index_name, status FROM dba_indexes
  2  WHERE  owner = 'NET19B';

INDEX_NAME   STATUS
------------ --------
IDX_SRC      VALID

-- LONG 타입은 오래된 절차서에 "NETWORK_LINK 불가" 로 적혀 있다. 19c 에서 확인한다.

SYS@orcl> CREATE TABLE net19.has_long (id NUMBER, txt LONG)
  2    TABLESPACE users;
SYS@orcl> INSERT INTO net19.has_long VALUES (1, 'sample long value');
SYS@orcl> COMMIT;

[oracle@oel7v9 ~]$ impdp system/oracle_4U directory=dp_dir \
>   network_link=src_link tables=net19.has_long \
>   remap_schema=net19:net19b logfile=net_long.log

Starting "SYSTEM"."SYS_IMPORT_TABLE_01":  system/******** directory=dp_dir network_link=src_link tables=net19.has_long remap_schema=net19:net19b logfile=net_long.log
Estimate in progress using BLOCKS method...
Processing object type TABLE_EXPORT/TABLE/TABLE_DATA
Total estimation using BLOCKS method: 64 KB
Processing object type TABLE_EXPORT/TABLE/TABLE
. . imported "NET19B"."HAS_LONG"                              1 rows
Processing object type TABLE_EXPORT/TABLE/STATISTICS/TABLE_STATISTICS
Processing object type TABLE_EXPORT/TABLE/STATISTICS/MARKER
Job "SYSTEM"."SYS_IMPORT_TABLE_01" successfully completed at Wed May 21 17:22:15 2025 elapsed 0 00:00:08

-- 그대로 옮겨졌다. 12.2 부터 LONG/LONG RAW 도 NETWORK_LINK 로 지원된다.
-- 12.1 이하에서는 ORA-31696 (unable to export/import TABLE_DATA ... using client specified AUTOMATIC method) 으로
-- 그 테이블만 실패했다. 오래된 절차서의 제약 목록은 버전을 확인하고 읽는다.

-- 덤프 파일 방식도 물론 된다.

[oracle@oel7v9 ~]$ expdp system/oracle_4U directory=dp_dir \
>   dumpfile=lng.dmp logfile=lng.log tables=net19.has_long

. . exported "NET19"."HAS_LONG"                          5.484 KB       1 rows
Job "SYSTEM"."SYS_EXPORT_TABLE_01" successfully completed at Wed May 21 17:23:40 2025 elapsed 0 00:00:11

-- 정리

SYS@orcl> DROP PUBLIC DATABASE LINK src_link;
SYS@orcl> DROP USER net19 CASCADE;
SYS@orcl> DROP USER net19b CASCADE;

User dropped.

-- 실무 포인트
-- 1) NETWORK_LINK는 덤프 파일을 만들지 않는다.
--    DUMPFILE과 함께 쓰면 UDI-00011로 거부된다(작업이 만들어지기 전 클라이언트 검사).
--    디스크 여유가 없을 때 유용한 방식이다.
--
-- 2) 재시도가 어렵다는 점이 가장 큰 단점이다.
--    중단되면 남은 것이 없어 처음부터 다시 해야 한다.
--    네트워크가 불안정하거나 대용량이면 덤프 파일 방식이 안전하다.
--
-- 3) 링크의 접속 계정 권한이 그대로 적용된다.
--    원본에서 읽을 권한이 없으면 그 객체가 빠진다.
--    권한 있는 계정으로 링크를 만들되 비밀번호 관리에 유의한다.
--
-- 4) 타입 제약은 버전에 따라 다르다.
--    LONG/LONG RAW 는 12.2 부터 NETWORK_LINK 로 옮겨진다. 그 이전은 ORA-31696 이었다.
--    제약 목록은 쓰는 버전의 Utilities 문서로 확인한다.
--
-- 5) VERSION은 내보낼 때 지정한다.
--    반입 시점에 지정하는 것이 아니다.
--    이미 만든 덤프를 하위 버전에 넣을 수는 없고 다시 내보내야 한다.
--    덤프 형식 버전은 DBMS_DATAPUMP.GET_DUMPFILE_INFO 로 파일 헤더에서 읽을 수 있다.
--
-- 6) 하위에서 상위로는 조치가 필요 없다.
--    19c 덤프를 21c에 넣는 것은 그대로 된다.
--    방향을 혼동하지 않도록 절차서에 명시해 둔다.
