================================================================================
 실습 7-6 : CURRENT 리두 로그 파일 삭제
 과정      : Oracle Database 19c Backup & Recovery
 환경      : Oracle 19.3.0 / Oracle Linux 7 / non-CDB ORCL / NOARCHIVELOG
 소요 시간 : 약 25분
================================================================================

--------------------------------------------------------------------------------
[1] 시나리오 개요
--------------------------------------------------------------------------------
-- CURRENT 그룹의 모든 멤버가 유실된 상태에서 인스턴스가 ABORT로 멈췄다.
-- 그 그룹에는 커밋되었으나 데이터파일에 반영되지 않은 변경이 들어 있다.
-- 아카이브 사본이 없으므로 그 변경을 되살릴 수 없다.
-- CLEAR 가 거부되는 것을 확인한 뒤 Cold Backup 전체 복원으로 돌아가고
-- 손실 범위를 산정해 보고한다.
-- 복구 유형 : 전체 복원(Cold Backup, 백업 이후 변경 손실)

--------------------------------------------------------------------------------
[2] 사전 조건
--------------------------------------------------------------------------------
-- 실습 7-5를 마친 상태에서 이어서 진행한다.
-- 실습 직전에 Cold Backup을 새로 받아 전체 복원 경로도 확보해 둔다.

[oracle@oel7v9 ~]$ sqlplus / as sysdba

SYS@orcl> SET LINESIZE 200
SYS@orcl> COL member FOR a44

SYS@orcl> CREATE TABLE hr.insa_2025(id NUMBER, order_date DATE) TABLESPACE users;
Table created.

SYS@orcl> INSERT INTO hr.insa_2025 VALUES (1, SYSDATE);
SYS@orcl> COMMIT;
SYS@orcl> ALTER SYSTEM CHECKPOINT;
System altered.

-- 여기까지는 데이터파일에 반영되었다. 이 시점의 Cold Backup 을 받아 둔다.
-- 뒤에서 전체 복원 경로로 쓴다. temp 파일은 백업에 넣지 않는다.

SYS@orcl> SHUTDOWN IMMEDIATE
SYS@orcl> !rm -rf /home/oracle/backup/ch07 && mkdir -p /home/oracle/backup/ch07
SYS@orcl> !cp -p /u01/app/oracle/oradata/orcl/*.dbf /u01/app/oracle/oradata/orcl/*.ctl \
                 /u01/app/oracle/oradata/orcl/*.log /home/oracle/backup/ch07/
SYS@orcl> !cp -p /u02/oradata/orcl/*.ctl /u02/oradata/orcl/*.log /home/oracle/backup/ch07/
SYS@orcl> !rm -f /home/oracle/backup/ch07/temp01.dbf
SYS@orcl> STARTUP

--------------------------------------------------------------------------------
[3] 초기 상태 확인
--------------------------------------------------------------------------------
SYS@orcl> SELECT group#, sequence#, status FROM v$log ORDER BY group#;

    GROUP#  SEQUENCE# STATUS
---------- ---------- ----------------
         1         13 CURRENT
         3         11 INACTIVE
         4         12 INACTIVE

-- 정상 종료 뒤 다시 열어도 CURRENT 그룹은 바뀌지 않는다. 시퀀스 13 을 이어 쓴다.

SYS@orcl> SELECT checkpoint_change# FROM v$database;

CHECKPOINT_CHANGE#
------------------
           2396040

SYS@orcl> SELECT id, TO_CHAR(order_date, 'HH24:MI:SS') AS t FROM hr.insa_2025;

        ID T
---------- --------
         1 20:04:12

-- 백업에 들어 있는 행이다.

--------------------------------------------------------------------------------
[4] 장애 유발 : CURRENT 그룹 전손 + ABORT
--------------------------------------------------------------------------------
SYS@orcl> INSERT INTO hr.insa_2025 VALUES (2, SYSDATE);
SYS@orcl> INSERT INTO hr.insa_2025 VALUES (3, SYSDATE);
SYS@orcl> COMMIT;
Commit complete.

SYS@orcl> SELECT id, TO_CHAR(order_date, 'HH24:MI:SS') AS t FROM hr.insa_2025 ORDER BY id;

        ID T
---------- --------
         1 20:04:12
         2 20:06:31
         3 20:06:31

-- 2번과 3번은 CURRENT 그룹(1번)의 리두에만 있다.

SYS@orcl> SELECT group#, status FROM v$log WHERE status = 'CURRENT';

    GROUP# STATUS
---------- ----------------
         1 CURRENT

SYS@orcl> !rm -f /u01/app/oracle/oradata/orcl/redo01.log /u02/oradata/orcl/redo01b.log

SYS@orcl> SHUTDOWN ABORT
ORACLE instance shut down.

--------------------------------------------------------------------------------
[5] 증상 관찰
--------------------------------------------------------------------------------
SYS@orcl> STARTUP
ORACLE instance started.
Database mounted.
ORA-00313: open failed for members of log group 1 of thread 1
ORA-00312: online log 1 thread 1: '/u01/app/oracle/oradata/orcl/redo01.log'
ORA-00312: online log 1 thread 1: '/u02/oradata/orcl/redo01b.log'
ORA-27037: unable to obtain file status
Linux-x86_64 Error: 2: No such file or directory
Additional information: 7

SYS@orcl> !tail -6 /u01/app/oracle/diag/rdbms/orcl/orcl/trace/alert_orcl.log
Beginning crash recovery of 1 threads
Started redo scan
Errors in file .../orcl_ora_18902.trc:
ORA-00313: open failed for members of log group 1 of thread 1
ORA-00312: online log 1 thread 1: '/u01/app/oracle/oradata/orcl/redo01.log'
ORA-313 signalled during: ALTER DATABASE OPEN...

--------------------------------------------------------------------------------
[6] 진단
--------------------------------------------------------------------------------
SYS@orcl> SELECT group#, sequence#, status FROM v$log ORDER BY group#;

    GROUP#  SEQUENCE# STATUS
---------- ---------- ----------------
         1         13 CURRENT
         3         11 INACTIVE
         4         12 INACTIVE

-- 손상된 그룹 1이 CURRENT다. 실습 7-5와 결정적으로 다른 지점이다.

SYS@orcl> SELECT group#, member, status FROM v$logfile WHERE status = 'INVALID';

    GROUP# MEMBER                                       STATUS
---------- -------------------------------------------- -------
         1 /u01/app/oracle/oradata/orcl/redo01.log      INVALID
         1 /u02/oradata/orcl/redo01b.log                INVALID

-- 멤버가 하나도 남지 않았다. 그 리두는 회복 불가다.

SYS@orcl> SELECT name, log_mode FROM v$database;

NAME      LOG_MODE
--------- ------------
ORCL      NOARCHIVELOG

-- 아카이브 사본도 없다.

-- 판정
--   ① 멤버 : CURRENT 그룹 전손, 생존 멤버 없음
--   ② 그룹 상태 : CURRENT → 미반영 커밋 변경이 그 안에 있음
--   ③ 종료 : abort → 인스턴스 복구에 그 그룹이 필요
--   → CLEAR 를 시도하되 거부되면 전체 복원. 백업 이후 변경의 손실 확정.

SYS@orcl> !mkdir -p /home/oracle/before_recover_76
SYS@orcl> !cp -p /u01/app/oracle/oradata/orcl/*.dbf \
                 /u01/app/oracle/oradata/orcl/*.ctl /home/oracle/before_recover_76/
SYS@orcl> !cp -p /u02/oradata/orcl/*.ctl /home/oracle/before_recover_76/

-- 되돌릴 수 없는 명령을 쓰기 전에 사본을 확보한다.

--------------------------------------------------------------------------------
[7] 복구 절차 : 오류 발생 → 원인 파악 → 수정
--------------------------------------------------------------------------------
-- 실습 7-4처럼 CLEAR 로 처리해 본다.

SYS@orcl> ALTER DATABASE CLEAR LOGFILE GROUP 1;
ALTER DATABASE CLEAR LOGFILE GROUP 1
*
ERROR at line 1:
ORA-01624: log 1 needed for crash recovery of instance orcl (thread 1)
ORA-00312: online log 1 thread 1: '/u01/app/oracle/oradata/orcl/redo01.log'

-- << 원인 파악 >>
-- 그룹 1은 CURRENT이며 인스턴스 복구에 필요한 리두를 담고 있다.
-- 실습 7-5에서는 손상된 그룹이 INACTIVE라 이 오류가 나오지 않았다.
-- 이번에는 손상된 그룹 자체가 인스턴스 복구에 필요하므로 거부된다.
-- 그런데 그 그룹의 멤버가 하나도 없어 리두를 읽을 방법이 없다.
-- 즉 CLEAR 없이는 진행할 수 없고 CLEAR 하면 리두를 잃는다.
-- 손실이 확정되는 지점이며, 그 사실을 인지한 상태에서 UNARCHIVED를 붙인다.

SYS@orcl> SELECT COUNT(*) FROM v$logfile WHERE group# = 1 AND status IS NULL;

  COUNT(*)
----------
         0

-- 살아 있는 멤버가 없음을 다시 확인했다.

-- << 수정 >>

SYS@orcl> ALTER DATABASE CLEAR UNARCHIVED LOGFILE GROUP 1;
ALTER DATABASE CLEAR UNARCHIVED LOGFILE GROUP 1
*
ERROR at line 1:
ORA-01624: log 1 needed for crash recovery of instance orcl (thread 1)
ORA-00312: online log 1 thread 1: '/u01/app/oracle/oradata/orcl/redo01.log'
ORA-00312: online log 1 thread 1: '/u02/oradata/orcl/redo01b.log'

-- UNARCHIVED 를 붙여도 같은 ORA-01624 다.
-- 이 옵션은 "아직 아카이브되지 않았다" 는 조건만 넘겨 준다.
-- "인스턴스 복구에 필요하다" 는 조건은 넘겨 주지 못한다. 두 조건은 별개다.
--
-- 여기서 길이 끊긴다.
--   · CLEAR 는 인스턴스 복구에 필요해서 거부된다.
--   · UNARCHIVED 를 붙여도 같은 이유로 거부된다.
--   · 그 리두를 읽을 방법은 없다. 멤버도 아카이브도 없다.
--   → 이 데이터베이스를 현재 시점으로 여는 방법은 존재하지 않는다.
--
-- 남은 선택지는 둘이다.
--   A. _allow_resetlogs_corruption 을 쓰는 비지원 절차.
--      오라클 지원 없이 단독으로 쓰지 않는다. 블록 손상을 남길 수 있고
--      그 뒤 데이터 정합성을 보장할 수 없다.
--   B. Cold Backup 전체 복원. 백업 시점으로 돌아가되 확실히 정상이 된다.
--
-- 실무 판단은 B 다. 손실 범위를 정확히 아는 상태로 돌아가는 것이,
-- 범위를 모르는 채 열린 데이터베이스보다 낫다.

-- << 수정 >> 전체 복원으로 전환한다.

SYS@orcl> SHUTDOWN ABORT
SYS@orcl> !cp -p /home/oracle/backup/ch07/*.dbf /u01/app/oracle/oradata/orcl/
SYS@orcl> !cp -p /home/oracle/backup/ch07/control01.ctl \
                 /home/oracle/backup/ch07/control02.ctl /u01/app/oracle/oradata/orcl/
SYS@orcl> !cp -p /home/oracle/backup/ch07/control03.ctl /u02/oradata/orcl/
SYS@orcl> !cp -p /home/oracle/backup/ch07/redo01.log \
                 /home/oracle/backup/ch07/redo03.log \
                 /home/oracle/backup/ch07/redo04.log /u01/app/oracle/oradata/orcl/
SYS@orcl> !cp -p /home/oracle/backup/ch07/redo01b.log \
                 /home/oracle/backup/ch07/redo03b.log \
                 /home/oracle/backup/ch07/redo04b.log /u02/oradata/orcl/

SYS@orcl> STARTUP
ORACLE instance started.
Database mounted.
Database opened.

-- 복구 명령이 필요 없다. 정상 종료 시점의 일관된 집합(데이터파일·컨트롤파일·리두)을
-- 통째로 되돌렸기 때문이다. RECOVER 도 RESETLOGS 도 쓰지 않는다.
-- temp01.dbf 는 지운 적이 없고 복원한 컨트롤파일에도 그대로 등록돼 있으므로
-- 다시 추가하지 않는다. 추가하면 ORA-01537 이다.

--------------------------------------------------------------------------------
[8] 검증 및 실무 포인트
--------------------------------------------------------------------------------
SYS@orcl> SELECT * FROM v$recover_file;

no rows selected

SYS@orcl> SELECT id, TO_CHAR(order_date, 'HH24:MI:SS') AS t FROM hr.insa_2025 ORDER BY id;

        ID T
---------- --------
         1 20:04:12

-- 2번과 3번 행이 사라졌다. 백업 뒤에 커밋한 변경이고 그 리두는 CURRENT 그룹에만 있었다.
-- 커밋했음에도 잃었다는 점이 이 실습의 핵심이다.

SYS@orcl> SELECT MAX(order_date) AS last_order FROM hr.insa_2025;

LAST_ORDER
---------------------
10-MAY-25 08.04.12 PM

-- 손실 구간 : 백업 시점(20:04:12 직후) 이후 ~ 장애 시각(20:06:31) 까지의 업무 전량
-- 이 정보를 현업에 통보하고 재처리 계획을 세운다.

SYS@orcl> SELECT incarnation#, resetlogs_change#, status FROM v$database_incarnation
  2  WHERE  status = 'CURRENT';

INCARNATION# RESETLOGS_CHANGE# STATUS
------------ ----------------- -------
           2           2393981 CURRENT

-- 올라가지 않았다. 전체 복원이라 RESETLOGS 를 쓰지 않았고,
-- 백업 시점의 컨트롤파일을 그대로 되돌렸기 때문이다.

SYS@orcl> SELECT group#, sequence#, status FROM v$log ORDER BY group#;

    GROUP#  SEQUENCE# STATUS
---------- ---------- ----------------
         1         13 CURRENT
         3         11 INACTIVE
         4         12 INACTIVE

-- 리두도 백업 시점 그대로다. RESETLOGS 를 쓰지 않았으므로 시퀀스가 1로 돌아가지
-- 않는다. 지웠던 그룹 1 은 백업본이 돌아와 다시 CURRENT 다.

-- 인카네이션은 그대로지만 데이터가 백업 시점으로 돌아갔다.
-- 복원에 써 버린 백업 하나만 남은 상태로 운영을 재개하지 않는다.
-- 즉시 전체 백업을 새로 받는다.

SYS@orcl> SHUTDOWN IMMEDIATE
SYS@orcl> !rm -rf /home/oracle/backup/ch07 && mkdir -p /home/oracle/backup/ch07
SYS@orcl> !cp -p /u01/app/oracle/oradata/orcl/*.dbf /u01/app/oracle/oradata/orcl/*.ctl \
                 /u01/app/oracle/oradata/orcl/*.log /home/oracle/backup/ch07/
SYS@orcl> !cp -p /u02/oradata/orcl/*.ctl /u02/oradata/orcl/*.log /home/oracle/backup/ch07/
SYS@orcl> !rm -f /home/oracle/backup/ch07/temp01.dbf
SYS@orcl> STARTUP

-- 실무 포인트
-- 1) 커밋되었어도 잃을 수 있다는 사실을 이 실습이 보여 준다.
--    커밋은 리두에 기록되었음을 보장할 뿐 데이터파일 반영을 보장하지 않는다.
--    그 리두가 사라지면 커밋된 변경도 되살릴 수 없다.
--
-- 2) 손상된 그룹이 CURRENT인지 INACTIVE인지가 결과를 가른다.
--    실습 7-5는 INACTIVE라 손실이 없었고 이 실습은 CURRENT라 손실이 생겼다.
--    같은 명령을 써도 결과가 다른 이유가 여기에 있다.
--
-- 3) ORA-01624가 나왔을 때 두 경우를 구분해야 한다.
--    손상된 그룹이 INACTIVE 면 손실을 인지한 상태에서 UNARCHIVED 를 붙여 CLEAR 한다.
--    손상된 그룹이 CURRENT 면 UNARCHIVED 를 붙여도 거부된다. 인스턴스 복구에
--    필요한 리두이기 때문이다.
--
-- 4) CURRENT 그룹의 멤버가 하나도 없으면 지원되는 경로는 전체 복원뿐이다.
--    복원 뒤에는 RECOVER 도 RESETLOGS 도 필요 없다. 정상 종료 시점의 일관된
--    집합을 통째로 되돌렸기 때문이다.
--
-- 5) 손실 구간은 반드시 업무 시각으로 환산해 통보한다.
--    백업 시각을 기점으로 잡고 업무 테이블의 최종 시각으로 검증한다.
--    기술 용어만으로 보고하면 현업이 재처리 범위를 판단할 수 없다.
--
-- 6) 예방책은 하나뿐이다. 멤버 다중화다.
--    멤버가 서로 다른 디스크에 하나라도 살아 있었다면 이 손실은 없었다.
--    ARCHIVELOG로 전환해도 CURRENT 리두는 아직 아카이브되기 전이라 보호되지 않는다.

SYS@orcl> EXIT
