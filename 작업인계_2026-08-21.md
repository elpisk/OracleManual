# 작업 인계 문서 — Oracle 교안 프로젝트 (2026-08-21 기준)

다른 창(새 세션)에서 이 프로젝트를 이어서 진행하기 위한 인계 문서다.
기존 `제작_가이드.md`(2026-08-12 스냅샷)는 **현황 표가 낡았다**. 규격·검증 절차는 여전히 유효하니 같이 읽되, 현재 진행 상태는 이 문서를 기준으로 삼는다.

---

## 0. 새 창에서 시작하는 법 (3분)

```powershell
cd C:\Users\itwill\Downloads\OracleManual
# PowerShell 세션마다 PATH 재로드 필요(git/python 인식 안 되는 경우)
$env:Path = [System.Environment]::GetEnvironmentVariable("Path","Machine") + ";" + [System.Environment]::GetEnvironmentVariable("Path","User")
git log --oneline -6 ; git status --short
```

먼저 읽을 파일 4개:
1. 이 문서
2. `제작_가이드.md` — 산출물 규격(3장) / 검증 절차(4장) / 시행착오 규칙(5장)
3. 해당 과정의 `*_챕터_목록.md`
4. 해당 과정의 `*_실습_트랜스크립트_주제_목록.md`

이전 대화를 그대로 이어받고 싶으면:
- `cd C:\Users\itwill\Downloads\OracleManual ; claude --resume 6f8ecb1e-2952-424d-b16a-b248ddb61684` (최신 세션, 8/21 16:55 KST 종료)
- `cd C:\Users\itwill ; claude --resume ee87315f-be58-4588-9e58-f8ae4d7eb292` (BR 본과정·고급실습 세션, 8/20 11:26 KST 종료)

---

## 1. 현재 상태 한 줄 요약

**ADMIN(34장) · PLSQL(7장) · BACKUP&RECOVERY(19장) 세 과정 본편이 모두 완료·push되어 있고, ADMIN과 BR에 대해 OCM Practicum 스타일 "최종 실습 시험" 8주제씩(문제지+모범답안 16파일씩)까지 완료·push된 상태다.** 진행 중이거나 중단된 작업은 없다.

---

## 2. 저장소·폴더 지도

| 구분 | 경로 |
|---|---|
| GitHub 저장소 | `elpisk/OracleManual` (public), 브랜치 `master` |
| 로컬 클론(정식본) | `C:\Users\itwill\Downloads\OracleManual\` |
| ADMIN 초안 작업 폴더 | `C:\Users\itwill\Downloads\oracle-admin-교안\` |
| PLSQL 초안 작업 폴더 | `C:\Users\itwill\Downloads\oracle-plsql-교안\` |
| BR 초안 작업 폴더 | `C:\Users\itwill\Downloads\oracle-br-교안\` |

작업 흐름은 항상 **초안 폴더에서 생성 → robocopy로 저장소 서브폴더에 동기화 → git add/commit/push**.

```powershell
# 주의: /MIR 금지, /E 사용 (4장 규칙 3번 참고)
robocopy "C:\Users\itwill\Downloads\oracle-br-교안" "C:\Users\itwill\Downloads\OracleManual\BACKUP&RECOVERY" /E /NFL /NDL /NJH /NJS
```

저장소 최상위 구성: `ADMIN/`, `BACKUP&RECOVERY/`, `PLSQL/`, `SQL/`, `linux_guide/`, `dashboard/`(미추적), `제작_가이드.md`, `README.md`, `LICENSE`.

---

## 3. 과정별 진행 현황 (2026-08-21)

| 과정 | 상태 | 산출물 |
|---|---|---|
| **ADMIN** (Oracle 19c DBA, 34장) | 완료·push | 34개 챕터 폴더 + `교안_챕터_목록.md` + `실습_트랜스크립트_주제_목록.md` + `최종실습시험/`(8주제 16파일) |
| **PLSQL** (7장) | 완료·push (커밋 `069613b`, 92개 파일) | 7개 챕터 폴더 + `PLSQL_PRD.md` + 챕터/트랜스크립트 목록 |
| **BACKUP&RECOVERY** (19장) | 완료·push | 19개 챕터 + 시나리오 PDF(BNR_01~16 등) + `종합실습과제/` + `고급실습_RecoveryCatalog/` + `최종실습시험/`(8주제 16파일) |
| **SQL** | 기존 완료본 (164개 파일, md/pptx) | 이번 작업 범위 아님 |
| **linux_guide** | 기존 완료본 (ch01~13 + appendix) | 이번 작업 범위 아님 |

### 3-1. BACKUP&RECOVERY 19장 구성

| 장 | 제목 |
|---|---|
| 1 | 백업·복구 개요와 19c 복구 아키텍처 |
| 2 | 복구 가능성 구성 — ARCHIVELOG·FRA·다중화 |
| 3 | User Managed 백업 — Cold Backup과 Hot Backup |
| 4~7 | NOARCHIVELOG 복구 ①~④ (데이터파일 / UNDO·TEMP·전체디스크 / 컨트롤파일 / 리두로그·복합장애) |
| 8~12 | ARCHIVELOG 복구 ①~⑤ (완전복구 / 전체데이터파일·아카이브결손 / 불완전복구 / CURRENT 리두·컨트롤파일 / 복합장애) |
| 13 | RMAN 개요·구성과 Recovery Catalog |
| 14 | RMAN 백업 — Full·Incremental·Image Copy·암호화 |
| 15 | RMAN 복구 시나리오 |
| 16 | RMAN 고급 복구와 블록 손상 복구 |
| 17 | 데이터베이스 복제 — Clone DB와 RMAN Duplicate |
| 18 | Flashback 기술 |
| 19 | Data Pump와 논리 백업 |

### 3-2. BR 부속 산출물

- `종합실습과제/` — `종합실습과제.docx`, `종합실습과제_모범답안.docx` (15과제 100점)
- `고급실습_RecoveryCatalog/` — 이수자 대상 심화. 트랜스크립트 10개(예제당 805~1,200줄, 총 28.5시간·5일 구성) + `고급실습_과정안내.docx` + `고급실습_RecoveryCatalog.pptx`(38슬라이드) + `운영스크립트/`(12종 + README)
  - 알려진 한계 2가지: (1) 예제 05의 `RMAN-06429`(카탈로그 버전 불일치)는 19.3.0 단일 환경이라 실행 재현이 불가능해 문서에 그렇게 명시했다. (2) 예제 06의 `RMAN-06511`(KEEP FOREVER를 nocatalog에서 사용) 코드 번호는 버전에 따라 다를 수 있어 실습 환경에서 한 번 확인이 필요하다.

### 3-3. 최종 실습 시험 (OCM DBA Practicum 스타일) — 8/21 완료

각 주제 폴더에 `{주제}_문제지.docx` + `{주제}_모범답안.docx` 2파일.

| ADMIN `최종실습시험/` | BR `최종실습시험/` |
|---|---|
| 01_Configuration | 01_BackupConfig |
| 02_Network | 02_NOARCHIVELOG |
| 03_Multitenant | 03_ARCHIVELOG |
| 04_Storage | 04_RMAN_구성백업 |
| 05_DataMgmt_Availability | 05_RMAN_복구고급 |
| 06_DataMovement | 06_Cloning |
| 07_자동유지관리 | 07_Flashback |
| 08_Performance | 08_DataPump |

ADMIN 34장 중 ASM(34장)은 시험 범위에서 제외했고, 백업 주제는 사용자 지시("백업은 제외해줘")로 ADMIN 세트에서 빼고 BR 세트로 분리했다.

---

## 4. 반드시 지킬 규칙 (사용자 지시 + 사고 사례)

1. **답변은 한글로** (2026-08-12 사용자 지시).
2. **원본 교재 PDF는 저장소에 커밋하지 않는다.** 사용자가 두 번 명시 지시했다 — `4. Backup&Recovery\BR1~3`(11g Oracle University 교재, "넣지 말것"), `PLSQL/PLSQL1.pdf`·`PLSQL2.pdf`("올리지 말것"). `.gitignore`에도 추가하지 않고 untracked 상태로 둔다. **다시 묻지 않는다.** 직접 만든 시나리오 PDF(`BNR_01~16`, `Clone DB.pdf`)는 이미 커밋되어 있으므로 "PDF 전부 금지"가 아니라 "원본 교재만 금지"다.
3. **robocopy는 `/E`를 쓴다. `/MIR` 금지** — `/MIR`는 대상 폴더에만 있는 소스 PDF를 지워버린다. (`제작_가이드.md` 2장에는 아직 `/MIR`로 적혀 있으나 BR 작업 중 바뀐 규칙이다.)
4. **에이전트 자체보고를 그대로 믿지 않는다.** "완료했다"는 보고는 오케스트레이터가 python-docx / python-pptx 스크립트로 직접 재검증한다(글자수·슬라이드수·노트 길이·문항수·정답 노출 여부). 실제로 4장 노트 길이 미달, 5장 EXIT 누락 등을 이 절차로 걸러냈다.
5. **PDF 부산물은 삭제하지 않는다.** 단, 스타일 분석용 스크래치 스크립트(.py 등)는 프로젝트 폴더에 남기지 말고 `%TEMP%`에서 작업한다(GitHub까지 올라간 사고가 있었다).
6. **세션/API 한도로 중단되면** 폴더 상태를 먼저 확인해 이미 완성된 파일은 재작업하지 않고, 남은 파일만 이어서 만드는 새 에이전트를 띄운다. 프롬프트에 "이 파일은 다시 만들지 말 것"을 명시한다.
7. **분량 미달이면** 이어붙이지 말고 처음부터 재작성한다(어중간하게 붙이면 스타일이 깨진다).
8. **버전 혼동 금지** — 소스가 11g인지 19c인지 항상 확인. 교안은 전부 19c 기준이다.
9. **PPT 발표자 노트 필수** — 타이틀/학습목표/Agenda/섹션구분/요약/실습개요를 제외한 모든 슬라이드에 200~500자 노트.
10. **인접 챕터와 역할 분담** — 새 챕터를 쓰기 전에 앞뒤 챕터 본문을 훑어 중복 여부를 확인한다.

---

## 5. 산출물 규격

### 5-1. 챕터 산출물 (ADMIN에서 확립, PLSQL·BR 동일 적용) — 챕터당 13~16파일

| 파일 | 규격 |
|---|---|
| `{N}장_{제목}.docx` | 20~30페이지 / **26,000~30,000자**. Heading1("제N장  제목")/2/3, 맑은 고딕 + Consolas 코드블록, Note(노랑)/Best Practice(녹색)/실무시나리오(파랑)/Quiz(보라) 박스(단일 셀 표), 퀴즈 6문항 + 요약 + 실습 개요로 마무리 |
| `{N}장_{제목}.pptx` | **35~55슬라이드**, 다크 타이틀/섹션 + 붉은 헤더(`#C0392B`) 콘텐츠 슬라이드. 노트 필수(규칙 9) |
| `{N}장_실습문제.docx` | 객관식 **50문항 (하20/중20/상10)**, 표기 `[하-01]`/`[중-01]`/`[상-01]`, 보기 `①②③④` |
| `{N}장_실습문제_정답.docx` | 정답 + 해설 |
| `{N}장_실습문제_추가서술형.docx` | **20문항**(서술형8 + 문제해결형6 + 명령어진행형6), 표기 `[추가-01]`, **객관식 절대 금지** |
| `{N}장_실습문제_추가서술형_정답.docx` | 모범답안 + 채점포인트 |
| `{N}장_실습_{NN}_{주제}.txt` | 세션 트랜스크립트(`SYS@cdb1>` 프롬프트, 한글 주석). 챕터당 3~10개. **각 파일 최소 1곳 "오류 발생 → 원인 파악 → 수정" 흐름 포함** |

BR 트랜스크립트는 추가로 **표준 8단계 템플릿**을 따른다(① 시나리오 개요에 복구 유형 명시 → … → ④ 장애 유발은 `rm`/`dd`/`drop` 등 재현 가능한 명령). 상세는 `BACKUP&RECOVERY\BR_챕터_목록.md` 2-1절에 있다.

### 5-2. 최종 실습 시험 문제지 규격 (OCM Practicum 스타일)

형식 근거 파일: `C:\Users\itwill\Downloads\01 Configuration_KKT.pdf`, `C:\Users\itwill\Downloads\03 Data Mgmt_KKT.pdf`. 이미지 기반 PDF라 텍스트 추출이 안 되고 PyMuPDF로 페이지를 렌더링해서 읽어야 한다.

- Word `.docx`. **실제 Word 헤더 기능** 사용 — 좌측 "Prep course for DBA Practicum", 우측 주제명. **푸터**는 "ITWILL + 페이지번호"
- 최상단 `■ {주제명}`(볼드) → `KEY WORDS`(볼드) + 핵심 키워드 한 줄 → 가로 구분선
- `Section N [소주제]`(볼드+밑줄)로 구분, 그 아래 `1`, `1.1`, `1.2` 번호식 과제 또는 OBJECT/SETTING 표(실제 Word 표 기능, 2~4열, 헤더 행 볼드)
- 전부 지시형 문장("~하시오"). **문제지에 정답 명령어·SQL·RMAN 문법을 절대 노출하지 않는다** — 수치/이름/경로/옵션 스펙만 명시한다
- 환경 가정: CDB "PROD"가 이미 존재, 신규 생성이 필요하면 "NEWDB", OS는 Oracle Linux, oracle 계정, `/u01/app/oracle` 표준 경로, 비밀번호 `oracle` 또는 `Oracle_123`
- 난이도: 숙련자 기준 **주제당 2~3시간**
- 모범답안은 문제지와 **동일한 Section/번호 구조**를 그대로 따라가며 실행 가능한 완전한 명령어(Consolas 고정폭) + 1~3줄 설명(주의사항/흔한 실수)을 덧붙인다

검증 포인트: 헤더/푸터가 본문 텍스트가 아니라 실제 Word 헤더/푸터인지, 문제지에 정답이 노출된 곳이 없는지, 모범답안 명령어가 19c 문법으로 정확한지, 분량이 2~3시간에 맞는지.

---

## 6. 작업 방식 (에이전트 오케스트레이션)

- **작업 단위당 백그라운드 서브에이전트 1개**(챕터 1개 = 에이전트 1개, 시험 주제 1개 = 에이전트 1개)로 전체 산출물을 한 번에 생성하게 한다. 초반에 3개로 쪼갰다가 승인 피로 때문에 통합했다.
- 에이전트 프롬프트에는 항상 다섯 블록을 넣는다: ① 배경 ② 형식 스펙(정확히) ③ 내용 근거 파일 우선순위 ④ 산출물 경로·파일명 ⑤ 완료 후 스스로 확인할 것. 8/21 시험 생성에 쓴 프롬프트가 좋은 템플릿이다.
- 완료 보고를 받으면 **오케스트레이터가 직접 재검증**한다(이중 검증).
- 여러 건을 모아서 push한다(매 건 push하지 않음).
- 실제로 겪은 실패 유형: 세션 한도, 월 지출 한도, 스트림 워치독 스톨, 응답 중단. 이때는 규칙 6번대로 "남은 파일만 이어서" 에이전트를 재실행하면 잘 복구된다. 8/21 BR 시험 8종이 전부 이 방식으로 완성됐다.

---

## 7. git 이력 및 현재 상태

```
089b950  Add Backup&Recovery 최종 실습 시험 8종 (문제지+모범답안 16파일)   <- HEAD, origin/master와 동일
d188b52  Add ADMIN 최종 실습 시험 8종 (문제지+모범답안 16파일)
0d13a36  Add Recovery Catalog 고급 실습 과정 (10개 예제)
2413142  Add Backup & Recovery 종합 실습 과제 2종
d759203  Add Backup & Recovery 과정 전체 산출물 (1~19장) 및 기획 문서
069613b  Add PLSQL 1~7장 전체 산출물 (92개 파일)
747a2a5  Add 제작_가이드.md
501fda3  Add ADMIN 29~34장 — 전체 34장 완료
```

미추적(untracked) 항목 — **의도적으로 그대로 둔 것들이니 손대지 말 것**:

```
?? 03 Data Mgmt_KKT.pdf      # 시험 형식 참고자료
?? PLSQL/PLSQL1.pdf          # 원본 교재 (규칙 2)
?? PLSQL/PLSQL2.pdf          # 원본 교재 (규칙 2)
?? dashboard/                # 강사용 대시보드 시안(8/20 작성, 이 교안 파이프라인과 무관)
```

---

## 8. 다음에 할 수 있는 일 (후보)

정해진 다음 작업은 없다. 자연스러운 후보는 다음과 같다.

1. **PLSQL 최종 실습 시험** — ADMIN·BR과 같은 OCM Practicum 스타일로 7장 기반 주제 세트 제작. 현재 PLSQL만 시험이 없다.
2. **SQL 과정 정비** — `SQL/`은 md/pptx 기반 구형 산출물이라 다른 과정의 docx 규격과 형식이 다르다.
3. **`제작_가이드.md` 현행화** — 1장 현황 스냅샷이 8/12 기준이고, 2장 robocopy가 `/MIR`로 되어 있어 위험하다(규칙 3).
4. **작업 폴더 정리** — `C:\Users\itwill\Downloads\` 아래 `_bnrtmp`, `_scratch_bnr`, `_scratch_br03`, `_scratch_render`, `_tmp_brt`, `_tmp_fb`, `_tmp_render`, `__pycache__` 등 스크래치 잔재가 남아 있다(저장소 밖이라 push에는 영향 없음).
5. **`dashboard/`** — 커밋할지 버릴지 결정 필요.

---

## 9. 잔재 정보 / 함정

- **세션 트랜스크립트 타임스탬프는 UTC**다. KST는 +9시간(8/21 16:55 KST 종료 = 로그상 07:55).
- 이전 세션 로그 위치: `C:\Users\itwill\.claude\projects\{경로slug}\{세션ID}.jsonl`
- 메모리 파일: `C:\Users\itwill\.claude\projects\C--Users-itwill\memory\` — `MEMORY.md`(인덱스), `project_oraclemanual_no_source_textbooks.md`(규칙 2), `feedback_avoid_ai_sounding_prose.md`(문체 — 재진술형 문단 끝맺음과 볼드 라벨 남발을 피할 것)
- PowerShell 새 세션마다 PATH 재로드가 필요할 수 있다(0장 명령 참고).
- 이미지 기반 PDF는 텍스트 추출이 안 되므로 PyMuPDF로 페이지를 렌더링해 읽어야 한다.
