# Oracle 교안 제작 가이드 (도구 무관 — Claude Code / Antigravity 등 어떤 AI 코딩 도구에서도 이 문서만 보면 이어서 작업 가능)

이 문서는 `ADMIN`(Oracle 19c DBA, 34챕터) 교안을 만들며 확립된 산출물 규격·검증 절차·시행착오를 정리한 것이다.
다른 IDE/에이전트(Antigravity의 Gemini 기반 에이전트 포함)로 작업을 옮기더라도, **이 문서 + 각 과정의 `*_챕터_목록.md` + `*_실습_트랜스크립트_주제_목록.md`** 세 종류 파일만 읽으면 지금까지의 작업 맥락과 규칙을 그대로 재현할 수 있다.

---

## 1. 프로젝트 현황 스냅샷 (2026-08-12 기준)

| 과정 | 상태 | 위치 |
|---|---|---|
| **ADMIN** (Oracle 19c DBA, 34챕터) | **완료**, GitHub push 완료(최신 커밋 `501fda3`) | 초안: `C:\Users\itwill\Downloads\oracle-admin-교안\`<br>정식: `OracleManual\ADMIN\` |
| **PLSQL** (PL/SQL 7챕터, PRD 기반) | 준비만 완료(챕터/주제 목록 작성), 콘텐츠 착수 전 | 초안: `C:\Users\itwill\Downloads\oracle-plsql-교안\`<br>정식: `OracleManual\PLSQL\` (PRD 원본 위치) |
| **BACKUP&RECOVERY** | 폴더만 생성, 기획 미착수 | `OracleManual\BACKUP&RECOVERY\` (빈 폴더) |

GitHub 저장소: `elpisk/OracleManual` (public). 로컬 클론: `C:\Users\itwill\Downloads\OracleManual\`.

---

## 2. 폴더 구조 원칙

- **초안 작업 폴더**(`Downloads\oracle-{과정명}-교안\`)에서 챕터별로 만든 뒤, **robocopy `/MIR`로 git 저장소의 해당 서브폴더에 동기화** → `git add` → `commit` → `push`.
  ```powershell
  $src = "C:\Users\itwill\Downloads\oracle-admin-교안"
  $dst = "C:\Users\itwill\Downloads\OracleManual\ADMIN"
  robocopy $src $dst /MIR /NFL /NDL /NJH /NJS
  ```
  (PLSQL은 `oracle-plsql-교안` → `OracleManual\PLSQL`로 동일하게 적용)
- 챕터 폴더명: `01장`, `02장`, ... (두 자리 숫자 + "장"). 새 과정도 동일 규칙을 따른다.
- **PowerShell에서 매번 PATH를 다시 로드해야 git/python이 인식됨**(세션마다 초기화되는 환경):
  ```powershell
  $env:Path = [System.Environment]::GetEnvironmentVariable("Path","Machine") + ";" + [System.Environment]::GetEnvironmentVariable("Path","User")
  ```

---

## 3. 챕터당 산출물 규격 (ADMIN에서 확립, 신규 과정도 재사용)

| 파일 | 규격 |
|---|---|
| `{N}장_{제목}.docx` | 본문. **20~30페이지 / 26,000~30,000자**(공백 포함, 표·코드블록 텍스트 합산). Heading 1(챕터 제목, "제N장  제목" 형식) / Heading 2(절) / Heading 3. 본문 맑은 고딕, 코드블록 Consolas. Note(노랑)/Best Practice(녹색)/실무시나리오(파랑)/Quiz(보라) 색상 박스(단일 셀 표로 구현). 퀴즈 6문항 + 요약 + 실습 개요로 마무리 |
| `{N}장_{제목}.pptx` | **35~55슬라이드**(콘텐츠 분량에 비례 — 트랜스크립트 개수가 많은 챕터는 40~55). 다크 타이틀/섹션 슬라이드, 붉은 헤더(`#C0392B`) 콘텐츠 슬라이드. **⚠️ 타이틀/학습목표/Agenda/섹션구분/요약/실습개요를 제외한 모든 콘텐츠·코드·퀴즈 슬라이드는 발표자 노트(`slide.notes_slide.notes_text_frame.text`) 필수** — 200~500자 완전한 문장 |
| `{N}장_실습문제.docx` | 객관식 위주 **50문항 (하20/중20/상10)**. 문항 표기 `[하-01]`, `[중-01]`, `[상-01]`, 보기 `①②③④` |
| `{N}장_실습문제_정답.docx` | 정답 + 해설 |
| `{N}장_실습문제_추가서술형.docx` | **20문항**(서술형8 + 문제해결형6 + 명령어진행형6). 표기 `[추가-01]`. **객관식 절대 금지**(문제 파일 기준. 정답 파일 안의 열거기호 `①②③④`는 허용) |
| `{N}장_실습문제_추가서술형_정답.docx` | 모범답안 + 채점포인트 |
| `{N}장_실습_{NN}_{주제}.txt` | 세션 로그 트랜스크립트(SQL*Plus `SYS@cdb1>` 프롬프트 등, 한글 주석). 챕터별 개수는 `*_실습_트랜스크립트_주제_목록.md` 참고(콘텐츠 분량에 비례해 3~10개). **각 파일 최소 1곳 "오류 발생 → 원인 파악 → 수정" 흐름 포함**(실제 있음직한 ORA- 오류 사용) |

---

## 4. 검증 절차 (에이전트 자체보고를 절대 그대로 믿지 말고 매번 직접 재실행)

에이전트가 "완료했다"고 보고해도 반드시 독립적으로 재검증한다. Python(`python-docx`, `python-pptx`) 스크립트 템플릿:

```python
import docx, pptx, re

base = r"C:\...\{N}장"
prefix = "N장_제목"  # 실제 파일명 접두사

# 1) 본문 글자수 + Heading1 확인
d = docx.Document(base + rf"\{prefix}.docx")
text = "\n".join(p.text for p in d.paragraphs)
for t in d.tables:
    for row in t.rows:
        for cell in row.cells:
            text += cell.text
print("본문 글자수:", len(text))  # 26,000~30,000 범위인지 확인
print("첫 Heading1:", next((p.text for p in d.paragraphs if p.style.name=="Heading 1"), None))

# 2) PPT 슬라이드/노트 확인
p = pptx.Presentation(base + rf"\{prefix}.pptx")
empty_notes = []
has_notes = 0
for i, s in enumerate(p.slides, 1):
    if s.has_notes_slide and s.notes_slide.notes_text_frame.text.strip():
        has_notes += 1
    else:
        empty_notes.append(i)
print("총 슬라이드:", len(p.slides), "노트있음:", has_notes)
print("노트없는 슬라이드 번호:", empty_notes)  # 전부 타이틀/학습목표/Agenda/섹션/요약/실습개요인지 육안 확인

# 3) 실습문제 하/중/상 개수
d2 = docx.Document(base + rf"\{prefix.split('_')[0]}_실습문제.docx")
qtext = "\n".join(p.text for p in d2.paragraphs)
print("하:", len(set(re.findall(r'\[하-(\d+)\]', qtext))))
print("중:", len(set(re.findall(r'\[중-(\d+)\]', qtext))))
print("상:", len(set(re.findall(r'\[상-(\d+)\]', qtext))))

# 4) 추가서술형 20개 + 객관식 없음
d3 = docx.Document(base + rf"\{prefix.split('_')[0]}_실습문제_추가서술형.docx")
stext = "\n".join(p.text for p in d3.paragraphs)
print("추가:", len(set(re.findall(r'\[추가-(\d+)\]', stext))))
print("객관식기호:", sum(stext.count(c) for c in "①②③④"))  # 0이어야 함

# 5) 폴더 내 총 파일 수 + PDF/스크래치 잔재 확인
import glob
files = glob.glob(base + r"\*")
print("총 파일수:", len(files))
print("PDF/py 잔재:", [f for f in files if f.lower().endswith((".pdf", ".py"))])  # 비어있어야 함
```

체크리스트:
- [ ] 본문 글자수 26,000~30,000자
- [ ] PPT 슬라이드수 규격 범위 + 빈 노트가 전부 예외 슬라이드(타이틀/학습목표/Agenda/섹션구분/요약/실습개요)인지
- [ ] 하/중/상 = 20/20/10
- [ ] 추가서술형 20개, 문제 파일에 객관식 기호 없음
- [ ] 트랜스크립트 개수 일치, 각 파일 오류→수정 흐름 존재
- [ ] 폴더 내 총 파일 수 = 규격대로(스크래치 스크립트/PDF 잔재 없음)

---

## 5. 시행착오에서 얻은 규칙 (반드시 지킬 것)

1. **버전 혼동 금지**: 소스가 11g인지 19c인지 항상 명시적으로 확인. (1장을 11g 기준으로 잘못 써서 전면 재작성한 사고 있었음)
2. **발표자 노트 필수**: 콘텐츠/코드/퀴즈 슬라이드에 노트가 비면 안 됨. (5장/6장에서 전부 누락되어 재작업)
3. **에이전트 자체보고는 검증 전까지 신뢰하지 않는다**: "다른 챕터 파일에 문제가 있다"는 식의 부수적 보고도 반드시 직접 재확인(반복적으로 착오였음).
4. **PDF 부산물은 삭제하지 않는다**: 작업 중 우연히 생기는 PDF 변환본은 그대로 둔다(사용자 명시적 지시, 2026-08-05경 확정). 단, **스타일 분석용 스크래치 스크립트(.py 등)는 프로젝트 폴더에 남기지 말고 `%TEMP%`에서 작업 후 정리**할 것(이건 PDF와 다른 문제 — GitHub까지 올라간 사고 있었음).
5. **세션/API 한도로 중단될 때**: 폴더 상태(`Get-ChildItem`)를 먼저 확인해 이미 완성된 파일은 재작업하지 않고, 남은 파일만 이어서 만드는 새 에이전트를 실행한다. 본문 docx가 완성돼 있으면 "이 파일은 다시 만들지 말 것"을 프롬프트에 명시.
6. **분량 미달 시**: 기존 미완성 파일을 무시하고 처음부터 재작성 지시(어중간하게 이어붙이면 스타일이 깨짐).
7. **PPT는 본문 다음으로 최우선 완성/저장**: 여러 챕터에서 PPT 빌드 단계 도중 API 오류로 중단된 사례가 많았음. `prs.save()`를 빌드 완료 즉시 호출.
8. **스타일은 항상 직전 완성 챕터의 실제 파일을 실측**해서 재사용(하드코딩된 값 나열 대신 `python-docx`/`python-pptx`로 직접 읽어 확인) — 하지만 이 실측 단계에 시간을 너무 쓰면 세션 한도에 걸리므로 간결하게.
9. **원본 소스 PDF에 해당 챕터 본문이 없는 경우** (예: `19c-Admin_01_image.pdf`는 볼륨 1(1~13장)만 수록): 목차만 확인하고, 나머지는 공식 문서 수준의 정확한 기술 지식 + 챕터 목록에 정의된 핵심 개념 골격으로 작성. 뒷 챕터(14~34장) 전부 이 방식으로 완료·검증됨.
10. **각 챕터는 앞뒤 챕터와 내용이 중복되지 않도록 역할을 분담**(예: 29장은 자동유지관리 "개요/조회", 30장은 "구성/변경" 중심으로 나눔). 새 챕터 작성 전 인접 챕터 본문을 훑어 중복 여부 확인.

---

## 6. 작업 방식 (에이전트 오케스트레이션)

- **챕터당 에이전트 1개**로 본문→PPT→실습문제 4종→트랜스크립트까지 전부 생성하도록 지시(초반엔 3개로 나눴으나 승인 피로 때문에 통합).
- 완료 후 **이 세션(오케스트레이터)에서 직접 재검증**(4장 참고) — 이중 검증 체계.
- 여러 챕터 완료 후 모아서 GitHub push(매 챕터 push 요청하지 않음).
- Antigravity 등 다른 도구에서는 "백그라운드 서브에이전트 + 완료 알림" 메커니즘이 Claude Code와 다를 수 있으니, 해당 도구의 방식에 맞게 조정하되 **"산출물 규격(3장) → 생성 → 직접 재검증(4장)"의 흐름 자체는 그대로 유지**할 것.

---

## 7. 다음 실행 시 참고할 파일

- `oracle-admin-교안\교안_챕터_목록.md`, `oracle-admin-교안\실습_트랜스크립트_주제_목록.md` — ADMIN 34챕터 전체 계획(완료 상태 반영됨)
- `oracle-plsql-교안\PLSQL_챕터_목록.md`, `oracle-plsql-교안\PLSQL_실습_트랜스크립트_주제_목록.md` — PLSQL 7챕터 계획(착수 전)
- `OracleManual\PLSQL\PLSQL_PRD.md` — PLSQL 과정 원본 기획서
- PLSQL 근거 자료 우선순위: `PLSQL1.pdf` / `PLSQL2.pdf`(1순위, `C:\Users\itwill\Downloads\`) → `database-pl-sql-language-reference.pdf`(보강) → PRD 골격 + 공식 지식
