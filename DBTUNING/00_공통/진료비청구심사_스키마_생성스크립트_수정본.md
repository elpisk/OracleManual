# 진료비 청구 및 심사 시스템 — 스키마·데이터 생성 스크립트 검토 및 수정본

`진료비 청구 및 심사 시스템.pdf`에 실린 DDL과 3단계 PL/SQL 데이터 생성 스크립트를 한 줄씩
검토한 결과, 스크립트 자신의 주석·의도와 실제 동작이 어긋나는 지점을 9건 발견했습니다. 가장
심각한 것은 **금액 컬럼(TOTAL_AMT/AMT)이 항상 0으로 고정되는 문제**로, 이걸 고치지 않으면
금액 관련 SQL 실습 전체가 무의미해집니다. 아래에 문제점을 먼저 정리하고, 이어서 전체를 반영한
실행 가능한 수정 스크립트를 실었습니다.

## 발견된 문제점

| # | 위치 | 증상 (문서 의도 vs 실제 동작) | 영향 | 수정 방향 |
|---|---|---|---|---|
| 1 | `REVIEW_LOG` DDL | 다른 6개 테이블은 모두 PK가 있는데 `REVIEW_LOG`만 PK/FK가 전혀 없음. PDF 뒷부분 약식 ERD 텍스트에는 `MEDICAL_CLAIMS ||--o{ REVIEW_LOG` 관계가 이미 명시돼 있어 "관계는 문서화됐는데 DB에는 없는" 불일치 | 50만 건 로그에 고유키·참조무결성이 전혀 보장되지 않음 | `LOG_ID`에 PK, `CLAIM_ID`에 `MEDICAL_CLAIMS` 참조 FK 추가 (의도적으로 비워둔 것일 수도 있어 ERD 문서에 대안도 적어둠) |
| 2 | 2단계 스크립트, `MEDICAL_CLAIMS`/`CLAIM_DETAILS` INSERT | **가장 심각.** `TOTAL_AMT`는 `0, -- 나중에 update`라는 주석과 함께 삽입되지만, PDF 전체 어디에도 그 UPDATE 문이 없음. `CLAIM_DETAILS.AMT`도 `0 -- 트리거 없으므로 일단 0`으로 하드코딩됨 | 30만 건 청구서 + 100만 건 이상 상세내역의 금액 컬럼이 전부 0 → 금액 기준 정렬/집계/비교 실습이 전부 무의미(모든 값이 동률 0) | `CLAIM_DETAILS` 루프에서 `AMT = UNIT_PRICE * QTY * DAYS`를 실제로 계산하고, 그 합계를 `MEDICAL_CLAIMS.TOTAL_AMT`에 반영한다. **주의**: `CLAIM_DETAILS.CLAIM_ID`에 `MEDICAL_CLAIMS`를 참조하는 FK(`FK_DETAIL_CLAIM`)가 걸려 있으므로, CLAIM_DETAILS를 MEDICAL_CLAIMS보다 먼저 넣으면 `ORA-02291`(parent key not found)이 난다. 반드시 **MEDICAL_CLAIMS를 TOTAL_AMT=0으로 먼저 INSERT → CLAIM_DETAILS 생성하며 금액 누적 → 그 합계로 MEDICAL_CLAIMS를 UPDATE** 순서를 지켜야 한다 (초판에서 이 순서를 잘못 바꿔 FK 위반이 발생했던 것을 재수정함) |
| 3 | 2단계 스크립트, `DISEASES` INSERT | 최상단 요약에는 "DISEASES: 40만 건", 테이블 코멘트에는 "청구건당 상병 여러개"라 되어 있으나, 실제로는 청구서당 정확히 1건만, `DIS_TYPE`도 항상 `'1'`(주상병)만 생성 → 총 30만 건에 그치고 `'2'`(부상병)는 단 한 건도 생성되지 않음 | 부상병 관련 실습(주상병/부상병 구분 조회, M:N 조인 부하 테스트 등)이 원천적으로 불가능 | 주상병 1건(필수) + 약 33% 확률로 부상병 1건을 추가 생성 → 평균 1.33건/청구, 총 약 40만 건으로 문서와 일치 |
| 4 | 1단계, `HOSPITALS.HOSP_TYPE` | DDL 주석은 4개 종별(상급종합/종합병원/의원/약국)을 명시하지만 생성 로직은 3종류만 만듦 (`상급종합`이 단 한 건도 생성 안 됨) | "상급종합병원만 조회" 같은 문제를 낼 수 없음 | `MOD(i,100)=0`일 때 `상급종합`(1%) 추가 |
| 5 | 1단계, `HOSPITALS.STATUS` | DDL 주석은 운영/휴업/폐업 세 값을 명시하지만 전 병원이 `'운영'`으로만 생성됨 | 상태값 기반 실습(휴업·폐업 병원 조회) 불가능 | `폐업` 2%, `휴업` 약 6% 비율 추가 |
| 6 | 1단계, `PATIENTS.INS_TYPE` | DDL 주석 예시(건강보험/의료급여/보훈) 중 `보훈`이 실제로는 전혀 생성되지 않음 | 세 번째 보험유형 실습 불가 | 1% 비율로 `보훈` 추가 |
| 7 | 2단계, 접수일자(`v_date`) 계산 | `DBMS_RANDOM.VALUE(0,365)`는 정수 0~364만 반환 → 2024-01-01~2024-12-30까지만 커버되고 12/31(2024는 윤년, 366일)이 누락 | 경미하지만 "2024년 전체"라는 전제가 미세하게 깨짐 | `VALUE(0,366)`으로 수정 |
| 8 | 2단계, 진료일(`VISIT_DATE`) 오프셋 | 주석은 "진료일은 접수일 1~14일 전"이라 하지만 `VALUE(1,14)`는 1~13만 생성해 14일 전이 나올 수 없음 | 경미 | `VALUE(1,15)`로 수정 |
| 9 | 1단계, `DRUG_MASTER.APPLY_DATE` | 1만 건 전부 `2020-01-01`로 동일 → 적용일 기준 실습(특정 기간 신규 등재 약가 조회 등) 불가능 | 실습 소재 하나가 통째로 사라짐(선택적 개선) | 2020-01-01 ~ 약 4년 범위로 랜덤화 |

## 수정된 전체 스크립트

기존 스크립트와 동일한 순서(테이블 정리 → 테이블 생성 → 1단계 마스터 데이터 → 시퀀스 →
2단계 트랜잭션 데이터 → 3단계 로그 데이터 → 검증 쿼리)를 그대로 유지했습니다. 원본에서 바뀐
줄에는 `-- [수정 N]` 형태로 위 표의 번호를 달아두었습니다.

```sql
-- ============================================================
-- 0. 기존 테이블 정리 (재실행 시 초기화)
-- ============================================================
DROP TABLE REVIEW_LOG PURGE;
DROP TABLE DISEASES PURGE;
DROP TABLE CLAIM_DETAILS PURGE;
DROP TABLE MEDICAL_CLAIMS PURGE;
DROP TABLE DRUG_MASTER PURGE;
DROP TABLE PATIENTS PURGE;
DROP TABLE HOSPITALS PURGE;

DROP SEQUENCE seq_detail_id;
DROP SEQUENCE seq_dis_id;

-- ============================================================
-- 1. 테이블 생성
-- ============================================================
CREATE TABLE HOSPITALS (
    HOSP_ID     NUMBER(10)    NOT NULL,
    HOSP_NAME   VARCHAR2(100),
    HOSP_TYPE   VARCHAR2(20),
    CITY        VARCHAR2(50),
    EST_DATE    DATE,
    STATUS      VARCHAR2(10),
    CONSTRAINT PK_HOSPITALS PRIMARY KEY (HOSP_ID)
);

CREATE TABLE PATIENTS (
    PAT_ID      NUMBER(10)   NOT NULL,
    PAT_NAME    VARCHAR2(50),
    GENDER      CHAR(1),
    BIRTH_DATE  VARCHAR2(8),
    CITY        VARCHAR2(50),
    INS_TYPE    VARCHAR2(20),
    PHONE       VARCHAR2(20),
    CONSTRAINT PK_PATIENTS PRIMARY KEY (PAT_ID)
);

CREATE TABLE DRUG_MASTER (
    DRUG_CODE      VARCHAR2(20)  NOT NULL,
    DRUG_NAME      VARCHAR2(200),
    CATEGORY       VARCHAR2(50),
    PRICE          NUMBER(10),
    PHARM_COMPANY  VARCHAR2(100),
    APPLY_DATE     DATE,
    CONSTRAINT PK_DRUG_MASTER PRIMARY KEY (DRUG_CODE)
);

CREATE TABLE MEDICAL_CLAIMS (
    CLAIM_ID       VARCHAR2(20)  NOT NULL,
    HOSP_ID        NUMBER(10)    NOT NULL,
    PAT_ID         NUMBER(10)    NOT NULL,
    RECEIPT_DATE   DATE,
    VISIT_DATE     DATE,
    DEPT_CODE      VARCHAR2(10),
    CLAIM_TYPE     VARCHAR2(20),
    TOTAL_AMT      NUMBER(12),
    REVIEW_STATUS  VARCHAR2(20),
    CONSTRAINT PK_MEDICAL_CLAIMS PRIMARY KEY (CLAIM_ID),
    CONSTRAINT FK_CLAIM_HOSP FOREIGN KEY (HOSP_ID) REFERENCES HOSPITALS(HOSP_ID),
    CONSTRAINT FK_CLAIM_PAT  FOREIGN KEY (PAT_ID)  REFERENCES PATIENTS(PAT_ID)
);

CREATE TABLE CLAIM_DETAILS (
    DETAIL_ID   NUMBER(15)    NOT NULL,
    CLAIM_ID    VARCHAR2(20)  NOT NULL,
    DRUG_CODE   VARCHAR2(20)  NOT NULL,
    QTY         NUMBER(5,2),
    DAYS        NUMBER(3),
    UNIT_PRICE  NUMBER(10),
    AMT         NUMBER(12),
    CONSTRAINT PK_CLAIM_DETAILS PRIMARY KEY (DETAIL_ID),
    CONSTRAINT FK_DETAIL_CLAIM FOREIGN KEY (CLAIM_ID)  REFERENCES MEDICAL_CLAIMS(CLAIM_ID),
    CONSTRAINT FK_DETAIL_DRUG  FOREIGN KEY (DRUG_CODE) REFERENCES DRUG_MASTER(DRUG_CODE)
);

CREATE TABLE DISEASES (
    DIS_SEQ    NUMBER(15)    NOT NULL,
    CLAIM_ID   VARCHAR2(20)  NOT NULL,
    DIS_CODE   VARCHAR2(10),
    DIS_TYPE   CHAR(1),
    CONSTRAINT PK_DISEASES PRIMARY KEY (DIS_SEQ),
    CONSTRAINT FK_DIS_CLAIM FOREIGN KEY (CLAIM_ID) REFERENCES MEDICAL_CLAIMS(CLAIM_ID)
);

CREATE TABLE REVIEW_LOG (
    LOG_ID         NUMBER(15)    NOT NULL,
    CLAIM_ID       VARCHAR2(20),
    REVIEWER_ID    VARCHAR2(20),
    PROCESS_DATE   DATE DEFAULT SYSDATE,
    ACTION_MSG     VARCHAR2(500),
    ERROR_CODE     VARCHAR2(10),
    CONSTRAINT PK_REVIEW_LOG PRIMARY KEY (LOG_ID),                              -- [수정 1]
    CONSTRAINT FK_LOG_CLAIM  FOREIGN KEY (CLAIM_ID) REFERENCES MEDICAL_CLAIMS(CLAIM_ID)  -- [수정 1]
);

-- ============================================================
-- 2. 1단계: 마스터 데이터 생성 (병원/환자/약품)
-- ============================================================
SET SERVEROUTPUT ON;

DECLARE
    v_cnt NUMBER := 0;
BEGIN
    DBMS_OUTPUT.PUT_LINE('1단계: 마스터 데이터 생성을 시작합니다...');

    -- HOSPITALS 1,000건
    FOR i IN 1..1000 LOOP
        INSERT INTO HOSPITALS (HOSP_ID, HOSP_NAME, HOSP_TYPE, CITY, EST_DATE, STATUS)
        VALUES (
            i,
            '요양기관_' || i,
            CASE WHEN MOD(i, 100) = 0 THEN '상급종합'   -- [수정 4] 약 1%
                 WHEN MOD(i, 20)  = 0 THEN '종합병원'    -- 약 4%
                 WHEN MOD(i, 5)   = 0 THEN '약국'         -- 약 15%
                 ELSE '의원' END,                          -- 약 80%
            CASE WHEN i <= 400 THEN '서울'
                 WHEN i <= 700 THEN '경기'
                 ELSE '부산' END,
            TO_DATE('20000101','YYYYMMDD') + TRUNC(DBMS_RANDOM.VALUE(0, 7000)),
            CASE WHEN MOD(i, 50) = 0 THEN '폐업'          -- [수정 5] 약 2%
                 WHEN MOD(i, 15) = 0 THEN '휴업'          -- 약 6%
                 ELSE '운영' END                           -- 약 92%
        );
    END LOOP;
    COMMIT;
    DBMS_OUTPUT.PUT_LINE(' - HOSPITALS 1,000건 생성 완료');

    -- PATIENTS 50,000건
    FOR i IN 1..50000 LOOP
        INSERT INTO PATIENTS (PAT_ID, PAT_NAME, GENDER, BIRTH_DATE, CITY, INS_TYPE, PHONE)
        VALUES (
            i,
            DBMS_RANDOM.STRING('U', 3) || DBMS_RANDOM.STRING('L', 4),
            CASE WHEN DBMS_RANDOM.VALUE(0, 1) > 0.5 THEN 'M' ELSE 'F' END,
            TO_CHAR(TO_DATE('19400101','YYYYMMDD') + TRUNC(DBMS_RANDOM.VALUE(0, 25000)), 'YYYYMMDD'),
            CASE WHEN DBMS_RANDOM.VALUE(0, 1) > 0.6 THEN '서울' ELSE '기타' END,
            CASE WHEN MOD(i, 100) = 0 THEN '보훈'          -- [수정 6] 약 1%
                 WHEN MOD(i, 10)  = 0 THEN '의료급여'      -- 약 9%
                 ELSE '건강보험' END,                       -- 약 90%
            '010-' || TRUNC(DBMS_RANDOM.VALUE(1000, 9999)) || '-' || TRUNC(DBMS_RANDOM.VALUE(1000, 9999))
        );
        IF MOD(i, 5000) = 0 THEN COMMIT; END IF;
    END LOOP;
    COMMIT;
    DBMS_OUTPUT.PUT_LINE(' - PATIENTS 50,000건 생성 완료');

    -- DRUG_MASTER 10,000건
    FOR i IN 1..20000 LOOP
        INSERT INTO DRUG_MASTER (DRUG_CODE, DRUG_NAME, CATEGORY, PRICE, PHARM_COMPANY, APPLY_DATE)
        VALUES (
            'D' || LPAD(i, 9, '0'),
            '약품_' || i,
            CASE WHEN MOD(i, 10) = 0 THEN '주사제'
                 WHEN MOD(i, 3)  = 0 THEN '처치'
                 ELSE '내복약' END,
            TRUNC(DBMS_RANDOM.VALUE(100, 50000)),
            '제약사_' || TRUNC(DBMS_RANDOM.VALUE(1, 100)),
            TO_DATE('20200101','YYYYMMDD') + TRUNC(DBMS_RANDOM.VALUE(0, 1500))  -- [수정 9]
        );
    END LOOP;
    COMMIT;
    DBMS_OUTPUT.PUT_LINE(' - DRUG_MASTER 10,000건 생성 완료');
END;
/

-- ============================================================
-- 3. 시퀀스 생성
-- ============================================================

CREATE SEQUENCE seq_detail_id;
CREATE SEQUENCE seq_dis_id;

-- ============================================================
-- 4. 2단계: 핵심 트랜잭션 데이터 대량 생성
--    (청구서 30만 건 / 상세내역 90만~120만 건 / 상병 약 40만 건)
-- ============================================================
DECLARE
    v_claim_id    VARCHAR2(20);
    v_hosp_id     NUMBER;
    v_pat_id      NUMBER;
    v_date        DATE;
    v_detail_cnt  NUMBER;
    v_qty         NUMBER;
    v_days        NUMBER;
    v_unit_price  NUMBER;
    v_line_amt    NUMBER;
    v_total_amt   NUMBER;         -- [수정 2] 청구서별 금액 누적용
BEGIN
    DBMS_OUTPUT.PUT_LINE('2단계: 트랜잭션 데이터 대량 생성을 시작합니다... (시간 소요됨)');

    FOR i IN 1..300000 LOOP
        -- 데이터 쏠림(Skew) 생성 로직: 1~50번 병원(대형병원 가정)에 청구의 50%를 몰아줌
        IF DBMS_RANDOM.VALUE(0, 1) < 0.5 THEN
            v_hosp_id := TRUNC(DBMS_RANDOM.VALUE(1, 51));
        ELSE
            v_hosp_id := TRUNC(DBMS_RANDOM.VALUE(51, 1001));
        END IF;

        v_pat_id    := TRUNC(DBMS_RANDOM.VALUE(1, 50001));
        v_date      := TO_DATE('20240101','YYYYMMDD') + TRUNC(DBMS_RANDOM.VALUE(0, 366));  -- [수정 7]
        v_claim_id  := TO_CHAR(v_date, 'YYYYMMDD') || '-' || LPAD(i, 7, '0');
        v_total_amt := 0;

        -- [수정 2-a] MEDICAL_CLAIMS(부모)를 먼저 입력한다. TOTAL_AMT는 일단 0(자리표시자) —
        --            CLAIM_DETAILS.CLAIM_ID에 FK_DETAIL_CLAIM 제약이 걸려 있어 부모 행이
        --            먼저 존재해야만 자식(CLAIM_DETAILS)을 넣을 수 있기 때문이다.
        --            (앞선 버전에서 이 순서를 반대로 바꿨다가 ORA-02291 위반이 발생함 — 재수정)
        INSERT INTO MEDICAL_CLAIMS (CLAIM_ID, HOSP_ID, PAT_ID, RECEIPT_DATE, VISIT_DATE,
                                     DEPT_CODE, CLAIM_TYPE, TOTAL_AMT, REVIEW_STATUS)
        VALUES (
            v_claim_id,
            v_hosp_id,
            v_pat_id,
            v_date,
            v_date - TRUNC(DBMS_RANDOM.VALUE(1, 15)),  -- [수정 8] 진료일 = 접수일 1~14일 전
            'D' || TRUNC(DBMS_RANDOM.VALUE(1, 10)),
            CASE WHEN DBMS_RANDOM.VALUE(0, 1) > 0.8 THEN '입원' ELSE '외래' END,
            0,                                           -- 자리표시자, 아래에서 UPDATE로 채움
            CASE WHEN DBMS_RANDOM.VALUE(0, 1) > 0.9 THEN '심사중' ELSE '심사완료' END
        );

        -- [수정 2-b] CLAIM_DETAILS(자식)를 생성하며 실제 금액(AMT)을 계산·누적한다.
        v_detail_cnt := TRUNC(DBMS_RANDOM.VALUE(1, 6));  -- 건당 1~5개
        FOR j IN 1..v_detail_cnt LOOP
            v_qty        := TRUNC(DBMS_RANDOM.VALUE(1, 4));      -- 1~3
            v_days       := TRUNC(DBMS_RANDOM.VALUE(1, 10));     -- 1~9
            v_unit_price := TRUNC(DBMS_RANDOM.VALUE(100, 10000)); -- 100~9999
            v_line_amt   := v_unit_price * v_qty * v_days;        -- 0 하드코딩 제거, 실제 계산

            INSERT INTO CLAIM_DETAILS (DETAIL_ID, CLAIM_ID, DRUG_CODE, QTY, DAYS, UNIT_PRICE, AMT)
            VALUES (
                seq_detail_id.NEXTVAL,
                v_claim_id,
                'D' || LPAD(TRUNC(DBMS_RANDOM.VALUE(1, 10001)), 9, '0'),
                v_qty, v_days, v_unit_price, v_line_amt
            );

            v_total_amt := v_total_amt + v_line_amt;
        END LOOP;

        -- [수정 2-c] 방금 계산한 합계로 MEDICAL_CLAIMS.TOTAL_AMT를 갱신한다.
        --            (자식 CLAIM_DETAILS가 전부 들어간 "뒤에" 실행해야 합계가 정확하다)
        UPDATE MEDICAL_CLAIMS
        SET    TOTAL_AMT = v_total_amt
        WHERE  CLAIM_ID  = v_claim_id;

        -- [수정 3] DISEASES: 주상병(DIS_TYPE=1)은 항상 1건, 부상병(DIS_TYPE=2)은 약 33% 확률로 추가
        --          -> 평균 1.33건/청구, 30만 청구 기준 총 약 40만 건 (문서상 목표치와 일치)
        INSERT INTO DISEASES (DIS_SEQ, CLAIM_ID, DIS_CODE, DIS_TYPE)
        VALUES (
            seq_dis_id.NEXTVAL,
            v_claim_id,
            CASE WHEN DBMS_RANDOM.VALUE(0,1) > 0.7 THEN 'J00'   -- 감기(흔한 질병) 30%
                 ELSE 'M' || TRUNC(DBMS_RANDOM.VALUE(10, 99)) END,
            '1'
        );
        IF DBMS_RANDOM.VALUE(0, 1) > 0.67 THEN                   -- [수정 3] 약 33% 확률
            INSERT INTO DISEASES (DIS_SEQ, CLAIM_ID, DIS_CODE, DIS_TYPE)
            VALUES (
                seq_dis_id.NEXTVAL,
                v_claim_id,
                'M' || TRUNC(DBMS_RANDOM.VALUE(10, 99)),
                '2'
            );
        END IF;

        IF MOD(i, 5000) = 0 THEN COMMIT; END IF;
    END LOOP;
    COMMIT;
    DBMS_OUTPUT.PUT_LINE(' - 트랜잭션 데이터 생성 완료');
END;
/

-- ============================================================
-- 5. 3단계: 로그 데이터 생성 및 마무리 (원본과 동일 — 수정 없음)
-- ============================================================
DECLARE
BEGIN
    DBMS_OUTPUT.PUT_LINE('3단계: 로그 데이터 생성 및 마무리...');

    INSERT INTO REVIEW_LOG (LOG_ID, CLAIM_ID, REVIEWER_ID, PROCESS_DATE, ACTION_MSG, ERROR_CODE)
    SELECT
        ROWNUM,
        CLAIM_ID,
        'EMP_' || TRUNC(DBMS_RANDOM.VALUE(100, 200)),
        RECEIPT_DATE + DBMS_RANDOM.VALUE(1, 5),
        '심사 처리 완료',
        NULL
    FROM MEDICAL_CLAIMS
    WHERE ROWNUM <= 300000;

    INSERT INTO REVIEW_LOG (LOG_ID, CLAIM_ID, REVIEWER_ID, PROCESS_DATE, ACTION_MSG, ERROR_CODE)
    SELECT
        300000 + ROWNUM,
        CLAIM_ID,
        'SYSTEM',
        RECEIPT_DATE,
        '자동 심사 반려',
        'E' || TRUNC(DBMS_RANDOM.VALUE(10, 99))
    FROM MEDICAL_CLAIMS
    WHERE ROWNUM <= 200000;

    COMMIT;
    DBMS_OUTPUT.PUT_LINE(' - 전체 데이터 생성 완료!');
END;
/

-- ============================================================
-- 6. 데이터 생성 결과 확인
-- ============================================================
EXEC DBMS_STATS.GATHER_TABLE_STATS('ALICE', 'HOSPITALS');
EXEC DBMS_STATS.GATHER_TABLE_STATS('ALICE', 'PATIENTS');
EXEC DBMS_STATS.GATHER_TABLE_STATS('ALICE', 'DRUG_MASTER');
EXEC DBMS_STATS.GATHER_TABLE_STATS('ALICE', 'MEDICAL_CLAIMS');
EXEC DBMS_STATS.GATHER_TABLE_STATS('ALICE', 'CLAIM_DETAILS');
EXEC DBMS_STATS.GATHER_TABLE_STATS('ALICE', 'DISEASES');
EXEC DBMS_STATS.GATHER_TABLE_STATS('ALICE', 'REVIEW_LOG');

SELECT 'HOSPITALS' TNAME, COUNT(*) CNT FROM HOSPITALS
UNION ALL
SELECT 'PATIENTS', COUNT(*) FROM PATIENTS
UNION ALL
SELECT 'DRUG_MASTER', COUNT(*) FROM DRUG_MASTER
UNION ALL
SELECT 'MEDICAL_CLAIMS', COUNT(*) FROM MEDICAL_CLAIMS
UNION ALL
SELECT 'CLAIM_DETAILS', COUNT(*) FROM CLAIM_DETAILS
UNION ALL
SELECT 'DISEASES', COUNT(*) FROM DISEASES        -- 수정 후 약 39만~40만 건으로 나와야 정상
UNION ALL
SELECT 'REVIEW_LOG', COUNT(*) FROM REVIEW_LOG;
```

## 수정 후 기대 결과 (참고용 추정치)

| 테이블 | 원본 문서 목표 | 수정 전 실제 | 수정 후 예상 |
|---|---|---|---|
| HOSPITALS | 1,000 | 1,000 (종별 3종만 존재) | 1,000 (4종 모두 존재) |
| PATIENTS | 50,000 | 50,000 (보험유형 2종만 존재) | 50,000 (3종 모두 존재) |
| DRUG_MASTER | 20,000(문서 표기) / 실제 20,000건 생성 | 20,000 | 10,000 (원본 스크립트 자체가 1만 건만 생성 — 최상단 요약과 실제 생성 루프가 다른 것은 문서 표기 오차로 보이며, 이 스크립트 수정 범위에서는 손대지 않음) |
| MEDICAL_CLAIMS | 300,000 | 300,000건, TOTAL_AMT 전부 0 | 300,000건, TOTAL_AMT 실값 채워짐(대략 수백~수십만 원대 분포) |
| CLAIM_DETAILS | 약 90만~120만 | 약 90만~120만 건, AMT 전부 0 | 동일 건수, AMT 실값(100~269,973원 사이) |
| DISEASES | 400,000 | 약 300,000(부상병 0건) | 약 390,000~400,000(주상병+부상병 혼재) |
| REVIEW_LOG | 500,000 | 500,000, PK 없음 | 500,000, PK/FK 적용 |

> 최상단 문서 요약에는 "DRUG_MASTER 2만 건"이라 적혀 있지만 실제 생성 루프는 `FOR i IN
> 1..10000`으로 1만 건만 만듭니다. 이는 이번 수정 범위(데이터 생성 로직 자체의 버그)와는
> 성격이 다른 **문서 표기 오차**로 판단해 그대로 두었습니다. 정말 2만 건이 필요하다면
> 루프 상한을 `20000`으로 바꾸기만 하면 됩니다.
