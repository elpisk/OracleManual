================================================================================
 Ch14 실습 03 — V$SQL 로 실행당 지표 뽑기와 튜닝 전후 기록 양식 (ORA-01476 교정 포함)
 목표: DISPLAY_CURSOR 없이도 V$SQL 의 누적 통계(BUFFER_GETS·ELAPSED_TIME·CPU_TIME·DISK_READS)
       를 EXECUTIONS 로 나눠 실행당 지표를 만든다. EXECUTIONS=0 인 커서에서 나는 ORA-01476 을
       재현·교정하고, 이 과정 전체에서 쓸 "튜닝 전/후 기록 양식" 을 한 SQL 로 채워 본다.
 환경: non-CDB=orcl 19.3, 스키마 SQLT.
 주의: 개인 실습 환경 전용. 조회만 수행한다.
================================================================================

$ sqlplus sqlt/sqlt@orcl

SQLT@orcl> SET LINESIZE 200


SQLT@orcl> SET PAGESIZE 100


--------------------------------------------------------------------------------
-- [1] (오류 → 원인 → 조치 → 재확인) 실행당 지표를 구하다 ORA-01476
--------------------------------------------------------------------------------
SQLT@orcl> -- 준비: 응용 프로그램이 문장을 prepare(파싱)만 하고 실행하지 않은 상황을 하나 만들어 둔다.
SQLT@orcl> --   (DBMS_SQL.PARSE 만 호출 — 실무에서는 실행 전에 취소된 요청, 준비만 해 둔 커서가 이렇게 남는다.)
SQLT@orcl> DECLARE
  2         c INTEGER := DBMS_SQL.OPEN_CURSOR;
  3       BEGIN
  4         DBMS_SQL.PARSE(c, 'SELECT claim_id FROM medical_claims WHERE claim_type = ''입원'' /* prepare only */',
  5                        DBMS_SQL.NATIVE);
  6         DBMS_SQL.CLOSE_CURSOR(c);
  7       END;
  8       /

PL/SQL procedure successfully completed.

SQLT@orcl> -- 관심 SQL 들: 이 과정에서 medical_claims 를 읽은 커서
SQLT@orcl> COLUMN sql_text FORMAT A50


SQLT@orcl> SELECT sql_id, executions,
  2              ROUND(buffer_gets  / executions)       gets_per_exec,
  3              ROUND(elapsed_time / executions / 1e3) ms_per_exec,
  4              SUBSTR(sql_text, 1, 50) sql_text
  5       FROM   v$sql
  6       WHERE  parsing_schema_name = 'SQLT'
  7       AND    UPPER(sql_text) LIKE '%MEDICAL_CLAIMS%'
  8       AND    UPPER(sql_text) NOT LIKE '%V$SQL%'
  9       ORDER  BY buffer_gets DESC;

            ROUND(buffer_gets  / executions)       gets_per_exec,
                               *
ERROR at line 2:
ORA-01476: divisor is equal to zero

SQLT@orcl> -- 원인 파악: EXECUTIONS=0 인 커서가 있다. 파싱은 됐지만 실행되지 않은 SQL(위에서 만든 prepare only
SQLT@orcl> --   커서처럼 실행 전에 취소된 요청)은 V$SQL 에 행이 있어도 EXECUTIONS 가 0 이다.
SQLT@orcl> SELECT sql_id, executions, parse_calls, SUBSTR(sql_text,1,50) sql_text
  2       FROM   v$sql
  3       WHERE  parsing_schema_name = 'SQLT' AND executions = 0
  4       AND    UPPER(sql_text) LIKE '%MEDICAL_CLAIMS%';

SQL_ID        EXECUTIONS PARSE_CALLS SQL_TEXT
------------- ---------- ----------- --------------------------------------------------
8awa0hdb066t7          0           1 SELECT claim_id FROM medical_claims WHERE claim_ty

SQLT@orcl> -- 조치: 0 으로 나누지 않게 NULLIF 로 막는다. 실행 0회 커서는 실행당 지표가 NULL 이 된다.
SQLT@orcl> SELECT sql_id, executions,
  2              ROUND(buffer_gets  / NULLIF(executions,0))       gets_per_exec,
  3              ROUND(elapsed_time / NULLIF(executions,0) / 1e3) ms_per_exec,
  4              ROUND(cpu_time     / NULLIF(executions,0) / 1e3) cpu_ms_per_exec,
  5              ROUND(disk_reads   / NULLIF(executions,0))       reads_per_exec,
  6              SUBSTR(sql_text, 1, 50) sql_text
  7       FROM   v$sql
  8       WHERE  parsing_schema_name = 'SQLT'
  9       AND    UPPER(sql_text) LIKE '%MEDICAL_CLAIMS%'
 10       AND    UPPER(sql_text) NOT LIKE '%V$SQL%'
 11       ORDER  BY buffer_gets DESC;

SQL_ID        EXECUTIONS GETS_PER_EXEC MS_PER_EXEC CPU_MS_PER_EXEC READS_PER_EXEC SQL_TEXT
------------- ---------- ------------- ----------- --------------- -------------- --------------------------------------------------
bwhftta2fqchp         10           727           1               1              0 SELECT /*+ GATHER_PLAN_STATISTICS */ CLAIM_ID, TOT
0mbcu5pt722m5          1          7270          13              12              0 BEGIN        FOR i IN 1..10 LOOP          FOR r IN
db60d4u6bqn70          1          3932          16              12              0 SELECT claim_id, total_amt FROM medical_claims WHE
dsd9jzuv8kc29          1          3117          17              16              0 SELECT /*+ GATHER_PLAN_STATISTICS */ claim_id, tot
66spw3bk8v13d          1          3093          23              21              8 SELECT /*+ GATHER_PLAN_STATISTICS */ h.hosp_type,
4r56zmmcbrmh7          1          3090          25              22              0 SELECT /*+ GATHER_PLAN_STATISTICS FULL(m) */ claim
2877sffy5ry7t          1          3090          10              10              0 SELECT COUNT(*) FROM medical_claims WHERE claim_ty
bguvzf2uspbr6          1          3090          10              11              0 SELECT COUNT(*) FROM medical_claims WHERE total_am
bskt2k8x8fytc          1          1891          15              14              0 SELECT /*+ GATHER_PLAN_STATISTICS */ COUNT(claim_i
7ct4wxtkuufp3          2           782           5               5            393 SELECT /*+ GATHER_PLAN_STATISTICS */ claim_id, tot
93wdbx5rrz6bf          1           781          41              39              0 SELECT /* DS_SVC */ /*+ dynamic_sampling(0) no_sql
8j88uxm6qhqb5          1           720           5               3              3 SELECT /*+ GATHER_PLAN_STATISTICS */ claim_id, tot
dwqugbkty8cac          1           428           2               3              0 SELECT /* DS_SVC */ /*+ dynamic_sampling(0) no_sql
4cpq5pz34s6fs          1           426           5               5              0 SELECT /* DS_SVC */ /*+ dynamic_sampling(0) no_sql
57axr0fpkcmby          1           394           4               4              0 SELECT /* DS_SVC */ /*+ dynamic_sampling(0) no_sql
bzadkkg2s9gxq          1           394           2               2              0 SELECT /* DS_SVC */ /*+ dynamic_sampling(0) no_sql
4y2t1tzudhr27          1           117           8               7              1 EXPLAIN PLAN FOR      SELECT h.hosp_type, COUNT(*)
3jg1290sn7sqt          1           103           6               6             57 DECLARE        c INTEGER := DBMS_SQL.OPEN_CURSOR;
a76m2sk0v5vqm          1            48           7               7              1 EXPLAIN PLAN FOR      SELECT claim_id, total_amt
2qjczxbt25vt7          1             7           2               1              1 SELECT /*+ GATHER_PLAN_STATISTICS */ claim_id
duq9848g2dvxy          1             4           2               1              0 SELECT /*+ GATHER_PLAN_STATISTICS FULL(medical_cla
a5yam95vzj6vs          1             4           3               2              0 EXPLAIN PLAN FOR SELECT claim_id FROM medical_clai
dqm5gdhthnrnn          1             4           2               2              0 EXPLAIN PLAN FOR SELECT claim_id FROM medical_clai
4sxqq3nxyvy7d          1             4           2               2              0 SELECT /*+ GATHER_PLAN_STATISTICS */ claim_id, tot
8awa0hdb066t7          0                                                          SELECT claim_id FROM medical_claims WHERE claim_ty

25 rows selected.

-- 재확인: 실행당 Buffers 가 DISPLAY_CURSOR 의 Buffers 와 같다(727 = 실습 02, 3,117 = Ch03 실습 01,
--   3,093 = Ch03 실습 02). Ch01 의 8,604·6,713 커서가 목록에 없는 것은 Ch02 실습 03 에서 공유 풀을
--   비웠기 때문 — 커서가 사라지면 V$SQL 의 통계도 함께 사라진다.
--   ms_per_exec 는 A-Time 과 같은 값이다(66spw3bk8v13d 20ms = Ch03 실습 02 의 A-Time 0.02초).
--   V$SQL 만으로도 "실행당 얼마" 를 알 수 있다 — 단, 행 단위(어느 단계가 비싼가)는 DISPLAY_CURSOR 로.

--------------------------------------------------------------------------------
-- [2] (조치 전 상태) 튜닝 전 기록 — 이 과정의 표준 양식
--------------------------------------------------------------------------------
-- 인덱스 설계(Ch05)·재작성(Ch08~12)·평가(Day 4) 에서 매번 이 양식을 채운다. 여기서는 Ch03 의
-- 카디널리티 오류 SQL 을 "튜닝 전" 으로 기록한다.
SQLT@orcl> SET AUTOTRACE TRACEONLY STATISTICS


SQLT@orcl> SELECT /*+ GATHER_PLAN_STATISTICS BEFORE_CH14 */ claim_id, total_amt
  2       FROM   medical_claims
  3       WHERE  claim_type = '입원' AND total_amt > 500000;

411 rows selected.


Statistics
----------------------------------------------------------
          1  recursive calls
          0  db block gets
       3117  consistent gets
       2367  physical reads
          0  redo size
      15417  bytes sent via SQL*Net to client
        811  bytes received via SQL*Net from client
         29  SQL*Net roundtrips to/from client
          0  sorts (memory)
          0  sorts (disk)
        411  rows processed

SQLT@orcl> SET AUTOTRACE OFF


SQLT@orcl> SELECT * FROM TABLE(DBMS_XPLAN.DISPLAY_CURSOR(NULL, NULL, 'ALLSTATS LAST +COST'));

PLAN_TABLE_OUTPUT
--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
SQL_ID  7bar1161xc3ah, child number 0
-------------------------------------
SELECT /*+ GATHER_PLAN_STATISTICS BEFORE_CH14 */ claim_id, total_amt
  FROM   medical_claims      WHERE  claim_type = '입원' AND total_amt >
500000

Plan hash value: 3132986698

--------------------------------------------------------------------------------------------------------------------
| Id  | Operation         | Name           | Starts | E-Rows | Cost (%CPU)| A-Rows |   A-Time   | Buffers | Reads  |
--------------------------------------------------------------------------------------------------------------------
|   0 | SELECT STATEMENT  |                |      1 |        |   857 (100)|    411 |00:00:00.07 |    3117 |   2367 |
|*  1 |  TABLE ACCESS FULL| MEDICAL_CLAIMS |      1 |  77946 |   857   (1)|    411 |00:00:00.07 |    3117 |   2367 |
--------------------------------------------------------------------------------------------------------------------

Predicate Information (identified by operation id):
---------------------------------------------------

   1 - filter(("CLAIM_TYPE"='입원' AND "TOTAL_AMT">500000))


20 rows selected.

--   기록 양식 (채운 예)
--   ┌─────────────────────┬───────────────────────────────┬──────────────────┐
--   │ 항목                │ 튜닝 전                       │ 튜닝 후          │
--   ├─────────────────────┼───────────────────────────────┼──────────────────┤
--   │ SQL_ID / PHV        │ 7bar1161xc3ah / 3132986698    │                  │
--   │ 최상위 Cost         │ 857                           │                  │
--   │ 최상위 E-Rows       │ 77,946                        │                  │
--   │ A-Rows              │ 411                           │                  │
--   │ Buffers (LAST)      │ 3,117                         │                  │
--   │ A-Time              │ 0.07초 (콜드 캐시)            │                  │
--   │ 핵심 Operation      │ TABLE ACCESS FULL             │                  │
--   │ access / filter     │ filter 만 (인덱스 없음)       │                  │
--   │ 캐시 상태           │ 콜드 (실습 01 FLUSH 뒤, Reads 2,367) │            │
--   └─────────────────────┴───────────────────────────────┴──────────────────┘
--   "튜닝 후" 열은 Ch05 에서 인덱스를 만든 뒤 같은 SQL 로 채운다. 계획이 바뀌면 PHV 가 달라진다.

--------------------------------------------------------------------------------
-- [3] (조치 후 관점) SQL 한 문장의 지표가 인스턴스 부문으로 이어지는 자리
--------------------------------------------------------------------------------
-- * 이 실습의 V$SQL 조회가 TUN 13장 실습 04(상위 SQL 선별)와 같은 조회다. 거기서는 스키마를
--   가리지 않고 인스턴스 전체에서 BUFFER_GETS·ELAPSED_TIME 상위를 뽑는다.
-- * ELAPSED_TIME 합계를 인스턴스 DB Time 으로 나누면 "이 SQL 이 DB Time 의 몇 %" 가 된다 —
--   브리지 01 의 징후 ②, 브리지 02 의 1 단계가 바로 이 계산이다.
-- * 둘 다 누적값이다. 구간 비교는 두 번 조회해 뺀다(TUN 5장) — 이 실습 02 의 ALL/LAST 와 같은 원칙.

--------------------------------------------------------------------------------
-- [정리]
--------------------------------------------------------------------------------
-- 1) V$SQL 의 누적 ÷ EXECUTIONS = 실행당 지표. EXECUTIONS=0 커서가 있으니 NULLIF 로 나눈다.
-- 2) 실행당 BUFFER_GETS 는 DISPLAY_CURSOR 의 Buffers 와 같다. 단계별 분해가 필요할 때만 DISPLAY_CURSOR.
-- 3) 튜닝 전/후 양식: SQL_ID·PHV·Cost·E-Rows·A-Rows·Buffers·A-Time·핵심 Operation·access/filter·캐시 상태.
-- 4) 같은 V$SQL 조회가 인스턴스 부문(TUN 13장, 브리지 02)에서 "상위 SQL 지목" 으로 쓰인다.

SQLT@orcl> EXIT

================================================================================
