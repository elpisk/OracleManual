# Oracle 19c DBA 교안 — 실습 트랜스크립트 주제 목록 (v3)

- **소스**: `19c-Admin_01_image.pdf` (챕터 1~33) + `automatic-storage-management-administrators-guide.pdf` (34장)
- 형식: SQL*Plus/쉘 세션 트랜스크립트 (프롬프트+명령어+실행결과+한글 주석, 오류→수정 흐름 포함)
- v2(원본 순서 33챕터) 대비: 31·32장을 1장 뒤로 이동, ASM을 34장으로 신규 추가, 전체 재넘버링

## 1장. Oracle 데이터베이스 소개 (3개)
1. Oracle 데이터베이스 서버 아키텍처(인스턴스+메모리+프로세스+저장소) 개요 확인
2. 멀티테넌트 컨테이너 데이터베이스(CDB) 소개 및 구성요소(root/PDB$SEED/PDB) 확인
3. 데이터베이스 샤딩(Sharding) 개념 확인

## 2장. 데이터베이스 프로세스 (8개)
1. 프로세스 아키텍처 및 구조 확인
2. DBWn(Database Writer)/LGWR(Log Writer) 프로세스 확인
3. CKPT(Checkpoint)/SMON/PMON 프로세스 확인
4. PMAN/RECO/LREG 프로세스 확인
5. MMON(Manageability Monitor) 프로세스 확인
6. ARCn(Archiver) 프로세스 확인
7. 메모리·프로세스·저장소 상호작용 확인
8. 백그라운드 프로세스 상태 조회(V$BGPROCESS) 실습

## 3장. 메모리 관리 (10개)
1. 공유 풀(Shared Pool) 구성요소 확인
2. 데이터베이스 버퍼 캐시 및 리두 로그 버퍼 확인
3. 라지 풀/자바 풀/스트림 풀 확인
4. PGA(Program Global Area) 구성 확인
5. 자동 메모리 관리(AMM) 활성화 실습
6. 자동 메모리 관리 모니터링 실습
7. 자동 공유 메모리 관리(ASMM) 구성 실습
8. 오라클 메모리 관련 파라미터(SGA_TARGET/MEMORY_TARGET 등) 비교 실습
9. PDB별 SGA 사용량 관리 실습
10. PDB별 PGA 사용량 관리 실습

## 4장. Oracle 데이터베이스 접속 (3개)
1. SQL*Plus로 OS 인증 및 Easy Connect 접속 실습
2. SQL*Plus에서 스크립트 호출(커맨드라인 인자/`@script`/쉘 스크립트) 실습
3. Oracle 데이터베이스 도구(SQL*Plus/SQL Developer/DBCA/EM Express) 비교 확인

## 5장. DBCA를 이용한 Oracle 데이터베이스 생성 (3개)
1. DBCA 대화형 모드로 데이터베이스 생성 실습
2. 문자집합(Character Set) 선택 및 NLS_LANG 설정 확인
3. DBCA Silent 모드로 CDB 생성(-createDatabase) 및 삭제(-deleteDatabase) 실습

## 6장. SQL 명령어를 이용한 Oracle 데이터베이스 생성 (3개)
1. CREATE DATABASE 문(ENABLE PLUGGABLE DATABASE)으로 CDB 생성 실습
2. SEED FILE_NAME_CONVERT 절로 시드 PDB 파일 위치 지정 실습
3. catcdb.sql 스크립트 실행으로 데이터 딕셔너리 구축 확인

## 7장. 데이터베이스 인스턴스 시작과 종료 (5개)
1. STARTUP NOMOUNT/MOUNT/OPEN 단계별 실습
2. SHUTDOWN 모드(ABORT/IMMEDIATE/NORMAL/TRANSACTIONAL) 비교 실습
3. ALTER PLUGGABLE DATABASE로 PDB OPEN/CLOSE 실습
4. PDB SAVE STATE/DISCARD STATE로 자동 오픈 구성 실습
5. SHUTDOWN ABORT 후 인스턴스 복구 시나리오 확인

## 8장. 데이터베이스 인스턴스 관리 (6개)
1. 초기화 파라미터 조회(SHOW PARAMETER, V$PARAMETER) 및 수정(ALTER SYSTEM/SESSION) 실습
2. SCOPE(MEMORY/SPFILE/BOTH) 및 DEFERRED 옵션 실습
3. ADR 구조 확인 및 알림로그(alert_SID.log) 조회 실습
4. 트레이스 파일 확인 및 DDL 로그(ENABLE_DDL_LOGGING) 활성화 실습
5. 동적 성능 뷰(V$SESSION, V$LOCK, V$SQL) 조회 실습
6. CDB_*/DBA_*/ALL_*/USER_* 데이터 딕셔너리 뷰 비교 실습

## 9장. Oracle Net Services 개요 (3개)
1. Oracle Net Services 구성요소 및 기본 리스너(LISTENER) 확인
2. 접속(Connection)과 세션(Session)의 차이 확인
3. 전용 서버(Dedicated) vs 공유 서버(Shared) 아키텍처 비교

## 10장. 네이밍 방법 구성 (5개)
1. Easy Connect 네이밍으로 접속 실습
2. Local Naming(tnsnames.ora) 구성 및 접속 실습
3. Directory Naming(LDAP) 개념 확인
4. 데이터베이스 서비스로 워크로드 관리 개념 확인
5. DBMS_SERVICE.CREATE_SERVICE로 서비스 생성 실습

## 11장. 리스너 구성 및 관리 (4개)
1. 기본 리스너 상태 조회(lsnrctl status) 실습
2. 동적 서비스 등록(LOCAL_LISTENER/REMOTE_LISTENER) 구성 실습
3. 정적 서비스 등록(SID_LIST_리스너명) 구성 실습
4. ALTER SYSTEM REGISTER로 즉시 서비스 등록 실습

## 12장. 공유 서버 아키텍처 구성 (4개)
1. SHARED_SERVERS 파라미터로 공유 서버 활성화 실습
2. DISPATCHERS/MAX_SHARED_SERVERS/SHARED_SERVER_SESSIONS/CIRCUITS 구성 실습
3. 공유 서버 환경에서 SGA/PGA(UGA) 사용 변화 확인
4. 공유 서버 사용이 제한되는 작업(DBA 작업/백업복구/배치) 확인

## 13장. 멀티플렉싱 및 접근 제어를 위한 Oracle Connection Manager 구성 (6개)
1. cman.ora 파일 구성(ADDRESS/RULE_LIST/PARAMETER_LIST) 실습
2. CMAN_RULES로 접근 제어 필터링 규칙(SRC/DST/ACT) 설정 실습
3. 클라이언트 tnsnames.ora에 Connection Manager 주소 설정 실습
4. 데이터베이스 서버에 REMOTE_LISTENER로 Connection Manager 등록 실습
5. DISPATCHERS의 MULTIPLEX 속성으로 세션 멀티플렉싱 구성 실습
6. CMCTL 유틸리티로 Connection Manager 시작/중지/상태 확인 실습

## 14장. Seed로부터 PDB 생성 (4개)
1. FILE_NAME_CONVERT 절로 PDB$SEED에서 새 PDB 생성 실습
2. 생성된 PDB 상태(NEW→NORMAL) 확인 및 OPEN 실습
3. OMF(DB_CREATE_FILE_DEST)를 이용한 PDB 생성 실습
4. PDB_FILE_NAME_CONVERT 파라미터를 이용한 PDB 생성 실습

## 15장. PDB 생성을 위한 기타 기법 활용 (10개)
1. CREATE PLUGGABLE DATABASE ... FROM으로 동일 CDB 내 PDB 복제 실습
2. DBMS_PDB.DESCRIBE로 non-CDB를 언플러그 PDB로 변환 실습
3. non-CDB를 CDB에 플러그인 후 noncdb_to_pdb.sql 실행 실습
4. 데이터베이스 링크를 이용한 원격 non-CDB/PDB 복제 실습
5. DBCA를 이용한 원격 PDB 복제(-createFromRemotePDB) 실습
6. PDB 언플러그(UNPLUG INTO) 후 다른 CDB에 플러그인 실습
7. 아카이브 파일(.pdb)을 이용한 PDB 언플러그/플러그 실습
8. Hot Cloning으로 운영 중인 원격 PDB 복제 및 증분 갱신(REFRESH) 실습
9. Near-Zero Downtime PDB 재배치(RELOCATE) 실습
10. Proxy PDB 생성 및 원격 PDB 조회(CONTAINERS 절) 실습

## 16장. PDB 관리 (6개)
1. PDB 모드 변경(READ WRITE/READ ONLY/MIGRATE) 실습
2. PDB 설정 변경 및 초기화 파라미터 영향 확인
3. PDB 내에서 ALTER SYSTEM 명령 사용 제약 확인
4. PDB별 호스트명/포트 번호 구성 실습
5. PDB 삭제(DROP PLUGGABLE DATABASE) 실습
6. PDB 초기화 파라미터 변경 전/후 비교 시나리오

## 17장. 데이터베이스 저장소 개요 (8개)
1. 데이터베이스 저장소 아키텍처 및 논리/물리 구조 확인
2. 세그먼트/익스텐트/블록 개념 확인
3. 테이블스페이스와 데이터 파일 관계 확인
4. CDB의 컨테이너별 기본 테이블스페이스(SYSTEM/SYSAUX) 확인
5. 세그먼트 유형별 저장 방식 확인
6. Deferred Segment Creation 활성화/비활성화 실습
7. 테이블스페이스 공간 사용량 모니터링 실습
8. 데이터베이스 블록 내용 구조 확인

## 18장. 테이블스페이스 생성 및 관리 (8개)
1. 테이블스페이스 생성 및 주요 절(BIGFILE/SMALLFILE 등) 실습
2. CDB에서 컨테이너별 영구 테이블스페이스 생성 실습
3. 기본 영구 테이블스페이스 지정 실습
4. 임시 테이블스페이스 생성 및 관리 실습
5. 테이블스페이스 변경(온라인/오프라인, 읽기전용) 및 삭제 실습
6. OMF(Oracle Managed Files)로 테이블스페이스 관리 실습
7. 데이터파일 추가/자동확장으로 데이터베이스 확장 실습
8. 온라인 데이터파일 이동 및 이름변경 실습

## 19장. 공간 사용 개선 (10개)
1. 블록 공간 관리 및 행 체이닝/마이그레이션 확인
2. 익스텐트 할당 및 사용 불가능한 인덱스 처리 실습
3. 글로벌 임시 테이블 생성 및 사용 실습
4. 프라이빗 임시 테이블(18c 신기능) 생성 및 사용 실습
5. 테이블 압축 개념 및 Direct-Path Insert 압축 실습
6. Advanced Row Compression(DML용) 적용 실습
7. 압축 어드바이저(Compression Advisor)로 압축률 예측 실습
8. 세그먼트 축소(SHRINK SPACE)로 공간 회수 실습
9. Resumable Space Allocation 활성화 및 일시중단된 문장 재개 실습
10. 재개 가능(Resumable) 작업 대상 확인

## 20장. 언두 데이터 관리 (8개)
1. 언두 데이터 개념 및 트랜잭션과의 관계 확인
2. 언두 데이터와 리두 데이터 비교
3. SHARED Undo Mode와 LOCAL Undo Mode(19c 기본) 비교 실습
4. 언두 보존 기간(Undo Retention) 설정 실습
5. 언두 보존 보장(GUARANTEE) 옵션 실습
6. 언두 테이블스페이스 고정 크기로 전환 실습
7. Temporary Undo(임시 언두) 활성화 및 이점 확인
8. Temporary Undo 모니터링 실습

## 21장. 사용자 계정 생성 및 관리 (7개)
1. 데이터베이스 사용자 계정 개념 및 Oracle 제공 관리자 계정 확인
2. 멀티테넌트 환경에서 사용자 생성(공통/로컬) 실습
3. CDB와 PDB에 공통 사용자 생성 실습
4. 스키마 전용 계정(Schema Only Account) 생성 실습
5. 비밀번호 인증 및 비밀번호 파일 인증 실습
6. OS 인증 및 권한 있는 사용자의 OS 인증 실습
7. 사용자 할당량(Quota) 지정 실습

## 22장. 권한 및 롤 인가 구성 (7개)
1. 시스템 권한과 객체 권한 부여/회수 실습
2. 멀티테넌트 환경에서 권한 부여(CONTAINER=ALL 등) 실습
3. 롤(Role) 생성 및 권한 할당 실습
4. Oracle 제공 롤 확인 및 사용 실습
5. 멀티테넌트 환경에서 롤 부여 실습
6. 보안 강화된 롤(비밀번호 필요 롤) 구성 실습
7. 롤과 시스템/객체 권한 회수 실습

## 23장. 사용자 자원 제한 구성 (6개)
1. 프로파일 개념 및 사용자 자원 제한 확인
2. 멀티테넌트 아키텍처에서 프로파일 생성 실습
3. 프로파일 자원 파라미터(세션/CPU 등) 구성 실습
4. 계정 잠금 및 비밀번호 관련 파라미터 구성 실습
5. Oracle 제공 비밀번호 검증 함수 적용 실습
6. 멀티테넌트 아키텍처에서 프로파일 할당 실습

## 24장. Oracle 데이터베이스 감사 구현 (10개)
1. 통합 감사(Unified Auditing) 정책 생성 기본 실습
2. 시스템 전반(System-Wide) 감사 옵션 설정 실습
3. 객체 지정(Object-Specific) 감사 액션 설정 실습
4. 조건(Condition)을 지정한 감사 정책 생성 실습
5. 감사 정책 활성화/비활성화 실습
6. CDB와 PDB에서의 감사 액션 확인
7. 통합 감사 정책 수정(ALTER AUDIT POLICY) 실습
8. 최상위 문장만 감사(Top-Level Statements Only) 설정 실습
9. Fine-Grained Auditing(FGA) 정책 생성 실습
10. 감사 추적 아카이빙 및 정리(Purging) 실습

## 25장. 데이터 적재 및 전송 소개 (3개)
1. 데이터 이동 아키텍처 개요 확인
2. Oracle Data Pump 개요 및 이점 확인
3. SQL*Loader 개요 확인

## 26장. 데이터 적재 (5개)
1. SQL*Loader 컨트롤 파일 작성 실습
2. SQL*Loader 적재 방식(전통적/다이렉트 패스) 비교 실습
3. 데이터 손실 방지 옵션 실습
4. SQL*Loader Express Mode 실습
5. PDB 내 테이블에 SQL*Loader로 데이터 적재 실습

## 27장. 데이터 전송 (10개)
1. Data Pump Export/Import 기본 실습(expdp/impdp)
2. Data Pump Import 변환(Transformation) 옵션 실습
3. PDB 환경에서 Data Pump 사용(FULL=Y 등) 실습
4. non-CDB에서 내보내 PDB로 가져오기 실습
5. PDB 간 데이터 내보내기/가져오기 실습
6. Full Transportable Export/Import 실습
7. 네트워크를 통한 데이터베이스 전송(NETWORK_LINK) 실습
8. RMAN CONVERT 명령으로 플랫폼 간 데이터 변환 실습
9. 이미지 카피/백업세트를 이용한 테이블스페이스 전송 실습
10. 엔디안 포맷 확인 및 불일치 테이블스페이스 전송 실습

## 28장. 외부 테이블을 이용한 데이터 적재 및 전송 (4개)
1. 외부 테이블(External Table) 생성 개념 확인
2. ORACLE_LOADER 액세스 드라이버로 외부 테이블 생성 실습
3. ORACLE_DATAPUMP 액세스 드라이버로 외부 테이블 생성/전송 실습
4. 외부 테이블 정보 조회(딕셔너리 뷰) 실습

## 29장. 자동 유지관리 작업: 개요 (5개)
1. 사전 예방적 유지관리 인프라 개요 확인
2. 사전 정의된 자동 유지관리 작업(옵티마이저 통계/Segment Advisor/SQL Tuning Advisor) 확인
3. 유지관리 윈도우 개념 및 사전 정의된 윈도우 확인
4. 유지관리 윈도우 세부정보 조회 실습
5. 자동 유지관리 작업 전체 흐름 확인

## 30장. 자동 유지관리 작업: 작업 및 윈도우 관리 (5개)
1. 자동 유지관리 작업 구성(DBMS_AUTO_TASK_ADMIN) 실습
2. 자동 유지관리 작업 활성화/비활성화 실습
3. 유지관리 윈도우 생성 및 관리 실습
4. 자동 유지관리 작업 리소스 할당 확인
5. 유지관리 작업 리소스 할당 변경 실습

## 31장. 데이터베이스 모니터링 및 성능 튜닝 개요 (6개)
1. 성능 관리 활동 및 계획 고려사항 확인
2. AWR(Automatic Workload Repository) 스냅샷 개념 확인
3. ADDM(Automatic Database Diagnostic Monitor) 분석 확인
4. PDB 수준에서 자동 ADDM 분석 구성 실습
5. Advisory Framework(어드바이저 종류) 확인
6. 성능 튜닝 방법론 확인

## 32장. 데이터베이스 성능 모니터링 (8개)
1. 서버 생성 알림(Server-Generated Alerts) 설정 실습
2. 메트릭 임계값(Metric Thresholds) 설정 실습
3. 알림 발생 시 대응 및 알림 유형/해제 실습
4. 데이터베이스 서버 통계/메트릭 조회 실습
5. 대기 이벤트(Wait Events) 모니터링 실습
6. 세션 모니터링 실습
7. 서비스(Services) 모니터링 실습
8. 통계 정보(V$SESSTAT 등) 조회 실습

## 33장. SQL 분석 및 액세스 경로 최적화 (8개)
1. SQL 튜닝 프로세스 및 옵티마이저 개요 확인
2. 옵티마이저 통계 수집 및 환경설정(Preferences) 실습
3. Optimizer Statistics Advisor로 통계 문제 분석 실습
4. SQL Plan Directives 및 Adaptive Execution Plans 확인
5. SQL Tuning Advisor 개요 확인
6. SQL Access Advisor 개요 확인
7. SQL Performance Analyzer 개요 확인
8. 자동 튜닝 작업(Automated Tuning Tasks) 관리 실습

## 34장. ASM 인스턴스 관리 (10개) ← ASM Administrator's Guide(19c) 근거
1. ASM 인스턴스 초기화 파라미터(ASM_DISKGROUPS, ASM_POWER_LIMIT 등) 구성
2. SYSASM/SYSDBA 권한 차이 및 ASM 인증 방식 실습
3. ASM 공유 비밀번호 파일 생성/관리 실습
4. 디스크 그룹 생성 및 리던던시/실패 그룹 구성 실습
5. 디스크 추가/삭제/리밸런싱/스크러빙 실습
6. 디스크 그룹 호환성(Compatibility) 속성 설정 실습
7. Oracle ASM Flex 디스크 그룹 및 파일 그룹/쿼터 그룹 구성 실습
8. ASM 파일명 규칙/별칭/템플릿 관리 실습
9. ASMCMD 유틸리티로 파일/디렉터리 관리 실습
10. RMAN을 이용한 ASM 데이터 마이그레이션 실습

---

## 총계
34개 챕터, **총 211개** 트랜스크립트 (챕터별 3~10개, 실제 콘텐츠 분량에 비례 배분)
