================================================================================
 Ch07 실습 03 — MEDICAL_CLAIMS(HOSP_ID) 인덱스를 만들면 힌트 없이도 NL: 3,093 → 44 (ORA-00904 교정)
 목표: 조인 컬럼에 인덱스를 만든 뒤 USE_NL 강제(Q4)와 힌트 없음(Q5)이 같은 NL 계획
       (Plan hash 872004878, Buffers 44)이 되는 것을 실측한다. Starts=5 로 NL 의 정의를 확인하고,
       컬럼명을 잘못 적어 나는 ORA-00904 를 교정한다. 마지막에 인덱스를 지운다.
 환경: non-CDB=orcl 19.3, 스키마 SQLT. DDL 포함.
 주의: 개인 실습 환경 전용. 실습 끝에 PK 만 남긴다.
================================================================================

$ sqlplus sqlt/sqlt@orcl

SQLT@orcl> SET LINESIZE 200
SQLT@orcl> SET PAGESIZE 100

--------------------------------------------------------------------------------
-- [1] (오류 → 원인 → 조치) 조인 컬럼 인덱스 생성 — ORA-00904
--------------------------------------------------------------------------------
SQLT@orcl> CREATE INDEX ix_ch07_hospid ON medical_claims(hospital_id);
CREATE INDEX ix_ch07_hospid ON medical_claims(hospital_id)
                                              *
ERROR at line 1:
ORA-00904: "HOSPITAL_ID": invalid identifier

SQLT@orcl> -- 원인 파악: 컬럼 이름이 HOSP_ID 다. HOSPITALS 의 PK 컬럼과 같은 이름이며 FK 컬럼이다.
SQLT@orcl> --   조인 컬럼에 인덱스를 만들 때는 조인 조건(h.hosp_id = m.hosp_id)에서 이름을 그대로 옮긴다.
SQLT@orcl> COLUMN column_name FORMAT A14
SQLT@orcl> SELECT column_name, data_type FROM user_tab_columns
  2       WHERE  table_name = 'MEDICAL_CLAIMS' AND column_name LIKE '%HOSP%';

COLUMN_NAME    DATA_TYPE
-------------- ----------
HOSP_ID        NUMBER

SQLT@orcl> CREATE INDEX ix_ch07_hospid ON medical_claims(hosp_id);

Index created.

--------------------------------------------------------------------------------
-- [2] (재확인) Q4 — USE_NL 강제, 인덱스 있음
--------------------------------------------------------------------------------
SQLT@orcl> SELECT /*+ GATHER_PLAN_STATISTICS USE_NL(h m) */ h.hosp_type, COUNT(*)
  2       FROM   hospitals h, medical_claims m
  3       WHERE  h.hosp_id = m.hosp_id
  4       AND    h.hosp_id <= 5
  5       GROUP  BY h.hosp_type;
(결과 실습 01 과 동일)
SQLT@orcl> SELECT * FROM TABLE(DBMS_XPLAN.DISPLAY_CURSOR(NULL, NULL, 'ALLSTATS LAST'));

PLAN_TABLE_OUTPUT
----------------------------------------------------------------------------------------------------------
Plan hash value: 872004878

----------------------------------------------------------------------------------------------------------
| Id  | Operation                            | Name           | Starts | A-Rows |   A-Time   | Buffers |
----------------------------------------------------------------------------------------------------------
|   0 | SELECT STATEMENT                     |                |      1 |      2 |00:00:00.01 |      44 |
|   1 |  HASH GROUP BY                       |                |      1 |      2 |00:00:00.01 |      44 |
|   2 |   NESTED LOOPS                       |                |      1 |  15029 |00:00:00.01 |      44 |
|   3 |    TABLE ACCESS BY INDEX ROWID BATCHED| HOSPITALS     |      1 |      5 |00:00:00.01 |       3 |
|*  4 |     INDEX RANGE SCAN                 | PK_HOSPITALS   |      1 |      5 |00:00:00.01 |       2 |
|*  5 |    INDEX RANGE SCAN                  | IX_CH07_HOSPID |      5 |  15029 |00:00:00.01 |      41 |
----------------------------------------------------------------------------------------------------------
   4 - access("H"."HOSP_ID"<=5)
   5 - access("H"."HOSP_ID"="M"."HOSP_ID")
       filter("M"."HOSP_ID"<=5)

Note
-----
   - this is an adaptive plan

-- 해석: NESTED LOOPS 가 나왔고 Buffers 3,093 → 44 (약 70배). Id 5 의 Starts=5 — 바깥(HOSPITALS) 5행
--   각각에 대해 안쪽 인덱스를 5번 탐색했다. NL 의 정의 그대로다. 15,029건을 인덱스 41블록으로 찾았다.
--   MEDICAL_CLAIMS 표 자체는 읽지 않았다(COUNT(*) 라 인덱스만으로 충분, Ch04 실습 03).
--   Ch03 실습 02 에서 filter 였던 M.HOSP_ID<=5 조건이 이제 인덱스 위에서 걸러진다.
--   "adaptive plan": 옵티마이저가 실행 중 실제 행 수를 보고 NL/HASH 를 고를 수 있게 준비한 계획이다
--   (TUN 15장 실습 08).

--------------------------------------------------------------------------------
-- [3] (재확인) Q5 — 힌트 없음, 인덱스 있음
--------------------------------------------------------------------------------
SQLT@orcl> SELECT /*+ GATHER_PLAN_STATISTICS */ h.hosp_type, COUNT(*)
  2       FROM   hospitals h, medical_claims m
  3       WHERE  h.hosp_id = m.hosp_id
  4       AND    h.hosp_id <= 5
  5       GROUP  BY h.hosp_type;
(결과 동일)
SQLT@orcl> SELECT * FROM TABLE(DBMS_XPLAN.DISPLAY_CURSOR(NULL, NULL, 'ALLSTATS LAST'));

PLAN_TABLE_OUTPUT
----------------------------------------------------------------------------------------------------------
Plan hash value: 872004878
(계획 Q4 와 동일 — NESTED LOOPS, Buffers 44, Starts=5, adaptive plan)

-- 해석: 힌트 없이도 Plan hash 872004878. 인덱스가 있으니 옵티마이저 스스로 NL 이 싸다고 판단했다.
--   실습 02 에서 힌트가 통하지 않았던 것과 대비된다 — 인덱스 유무가 힌트보다 결정적인 변수였다.

--   ┌────────────────────────────────┬────────────────────────┬─────────┐
--   │ 상황                           │ 조인 방식              │ Buffers │
--   ├────────────────────────────────┼────────────────────────┼─────────┤
--   │ 인덱스 없음, 힌트 없음 (Q1)    │ HASH JOIN              │  3,093  │
--   │ 인덱스 없음, USE_NL (Q2)       │ HASH JOIN (힌트 무시)  │  3,093  │
--   │ 인덱스 없음, USE_HASH (Q3)     │ HASH JOIN              │  3,093  │
--   │ 인덱스 있음, USE_NL (Q4)       │ NESTED LOOPS           │     44  │
--   │ 인덱스 있음, 힌트 없음 (Q5)    │ NESTED LOOPS (자동)    │     44  │
--   └────────────────────────────────┴────────────────────────┴─────────┘

--------------------------------------------------------------------------------
-- [4] 원복
--------------------------------------------------------------------------------
SQLT@orcl> DROP INDEX ix_ch07_hospid;

Index dropped.

SQLT@orcl> SELECT table_name, index_name FROM user_indexes WHERE index_name NOT LIKE 'PK%';

no rows selected

--------------------------------------------------------------------------------
-- [정리]
--------------------------------------------------------------------------------
-- 1) NL 조인은 "바깥 행마다 안쪽을 인덱스로 찾는" 방식이다. Starts 열이 그 반복 횟수다.
-- 2) 조인 컬럼(FK)에 인덱스가 생기자 힌트 없이도 NL 이 선택됐다 — 3,093 → 44.
-- 3) 조인 방식은 힌트로 고르는 것이 아니라 접근 경로(인덱스)를 주고 옵티마이저가 고르게 한다.
-- 4) FK 컬럼 인덱스는 조인뿐 아니라 부모 삭제 시 자식 표 잠금 범위에도 영향을 준다(TUN 14장 enq: TX).
-- 5) 인덱스를 지우고 PK 만 남았음을 확인했다.

SQLT@orcl> EXIT
================================================================================
